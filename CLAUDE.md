# CLAUDE.md — Panduan Proyek untuk Claude Code

> File ini dibaca otomatis oleh Claude Code di setiap sesi. Isinya konteks proyek, aturan, dan konvensi.
> Bagian bertanda **[ISI SETELAH AUDIT]** harus diperbarui setelah Claude Code memetakan repositori (lihat "Langkah pertama").

---

## 1. Gambaran Proyek

Aplikasi ini adalah **Portal Kinerja UNISAN** (`kinerja.unisan-g.id`). Kita sedang **menambahkan modul Presensi (absensi)** untuk **Dosen** dan **Pegawai/Staf**, yang diakses via **web dan smartphone**.

Tujuan modul presensi: karyawan bisa absen masuk/pulang dari perangkatnya, **dengan jaminan absen benar-benar dilakukan di kantor/kampus dan oleh orang yang bersangkutan** (anti-curang).

Modul presensi **menempel** pada aplikasi yang sudah ada — memakai ulang data pegawai, autentikasi/login, dan terhubung ke penilaian kinerja. **Jangan menduplikasi** data pegawai; referensikan model yang sudah ada. Data dosen bersumber dari **SIMDA** dan salinannya sudah tersimpan lokal di app **`simda_dosen`** pada portal kinerja — modul presensi mereferensikan model itu (lihat bagian 2.1).

---

## 2. Tech Stack

- **Backend:** Python + Django + Django REST Framework (DRF)
- **Database:** PostgreSQL biasa. ~~PostGIS~~ **tidak dipakai** — GDAL native library tidak tersedia di lingkungan dev proyek ini (lihat hasil audit Bagian 8), jadi cek geofence memakai `latitude`/`longitude` (`FloatField`) + formula **Haversine** murni-Python (`presensi/geo.py`), bukan `django.contrib.gis`/`PointField`. Cukup akurat untuk radius ratusan meter; revisit ke PostGIS kalau nanti ada kebutuhan query spasial yang lebih kompleks.
- **Tugas asinkron:** ~~Celery + Redis~~ **belum dipakai** — enrolment wajah (proses terberat) untuk sekarang dijalankan **sinkron** dalam request (skala ~150 dosen, enrolment jarang dilakukan, latensi beberapa detik dianggap wajar). Redis/Celery belum terpasang di VPS produksi; revisit kalau ternyata jadi masalah nyata (bukan Nixpacks/Railway — lihat catatan Deployment di bawah).
- **Face recognition:** InsightFace/ArcFace (`buffalo_l`, jalan di CPU via `onnxruntime`) — **sudah aktif** (`presensi/face.py`). Liveness/anti-spoof **BARU heuristik sederhana** (1 wajah terdeteksi, skor deteksi cukup, ukuran wajah wajar) — belum model anti-spoof ONNX terpisah seperti rencana awal.
- **Frontend:** PWA khusus `/presensi/` — **sudah ada** (`static/presensi-manifest.json` + service worker minimal di `/presensi/sw.js`, lihat § 9). Framework UI portal kinerja saat ini: **server-rendered Django templates + Tabler UI (Bootstrap-based, dimuat via CDN) + JS vanilla**. Tidak ada HTMX/Alpine/React yang terpasang — ikuti pola template+Tabler yang sama untuk halaman presensi non-PWA (mis. dashboard admin/HR).
- **Deployment:** ~~Railway~~ **VPS bare-metal** (`kinerja.unisan-g.id` di server `unisan-g`) — Nginx (proxy ke Unix socket) → `gunicorn-sikd.service` (systemd, jalan sebagai `root`) → kode di `/var/www/sikd` (clone git repo yang sama, di-update lewat `update.sh`: `git pull` + `pip install` + `migrate` + `collectstatic` + restart service, **dijalankan pakai `sudo`** karena file dimiliki `root`). `config/nixpacks.toml`/`Procfile` masih ada di repo tapi **tidak dipakai** untuk deployment aktual — sisa dari rencana awal, boleh diabaikan atau dihapus kalau memang tidak akan pindah ke Railway.

**[ISI SETELAH AUDIT]**
- Versi Django: **6.0.4** (di virtualenv proyek, `venv/`) · Versi Python: **3.12.3**. ⚠️ Ada instalasi Django global (5.1.15) di luar venv — **selalu jalankan lewat `venv/Scripts/python.exe` atau aktifkan venv dulu**, jangan pakai `python` sistem.
- App data dosen yang sudah ada: **`simda_dosen`**, model **`DataDosen`** (BUKAN `Dosen` — draft `presensi/models.py` & docs masih menyebut `simda_dosen.Dosen`, ini perlu dikoreksi). Field kunci: **`nidn`** = `CharField(max_length=20, unique=True, db_index=True)` — **sudah `unique=True`**, syarat FK/lookup terpenuhi. Model ini **`managed=False`**, tabel fisiknya `master"."data_dosen` di **database terpisah `unisan_db`** (bukan `sikd_db`), diakses lewat koneksi kedua `DATABASES['simda']` + `DATABASE_ROUTERS = ['config.db_router.SimdaRouter']`.
  - ⚠️ **Temuan penting:** `SimdaRouter.allow_relation()` (`config/db_router.py`) secara eksplisit **menolak relasi lintas-app** antara `simda_dosen` dan app lain (hanya mengizinkan relasi bila KEDUA sisi sama-sama `simda_dosen`). Karena `sikd_db` dan `unisan_db` adalah **database Postgres yang berbeda** (bukan sekadar schema berbeda), **`ForeignKey` biasa dari `presensi` ke `simda_dosen.DataDosen` tidak bisa bekerja** — Postgres tidak mendukung FK/JOIN lintas database, dan router memang sengaja melarangnya.
  - **Sudah diperbaiki** (lihat `presensi/models.py`, `presensi/utils.py`): semua field dosen di `presensi` sekarang `CharField nidn` biasa (bukan `ForeignKey`), diresolve manual lewat `get_dosen_by_nidn()` yang memanggil `DataDosen.objects.using('simda').filter(nidn=...)` — pola yang sama dipakai `profil/views.py` (helper `get_simda_dosen_or_none`/`dapat_kelola_nidn`).
- Mekanisme autentikasi: **Django session-based auth** (`accounts.User` custom model, `AUTH_USER_MODEL`), login via form username/password + captcha + lockout `django-axes` (`accounts/views.py::login_view`). Ditambah **`SSOAutoLoginMiddleware`** (`accounts/sso_middleware.py`) yang JIT-provisioning user dari cookie SSO eksternal (`sso_token`, diverifikasi ke `SSO_VERIFY_URL`). **Belum ada DRF maupun JWT** di proyek ini (`djangorestframework` & `djangorestframework-simplejwt` tidak ada di `requirements.txt`) — perlu ditambahkan khusus untuk API presensi (mobile/PWA butuh token, bukan cookie session SSO).

---

## 2.1 Sumber Data Dosen & Integrasi SIMDA (PENTING)

- **Master data dosen/pegawai ada di SIMDA** (database PostgreSQL tersendiri).
- Portal kinerja **sudah menyimpan salinan lokal** data dosen tersebut lewat app **`simda_dosen`** (dapat dilihat di `/admin/simda_dosen/`).
- **Modul presensi TIDAK menyambung langsung ke SIMDA** dan **TIDAK menduplikasi** data dosen.
- Presensi hanya menyimpan **hasil absensi** di tabelnya sendiri; identitas dosen (nama+gelar, fakultas/prodi versi SIMDA) diambil dari `simda_dosen` lewat NIDN saat dibutuhkan.
- Data dosen bersifat **read-only** bagi presensi — **jangan pernah menulis/mengubahnya** dari modul presensi.
- Hasil presensi terhubung ke penilaian kinerja lewat kunci yang sama (NIDN, diakses lewat `user.nidn`).

**[HASIL AUDIT — koreksi penting, sudah diterapkan di `presensi/models.py`]**
- Nama model: **`simda_dosen.DataDosen`** (bukan `Dosen`). Field kunci **`nidn`** sudah `CharField(unique=True, db_index=True)`.
- **`DataDosen` fisiknya di database terpisah (`unisan_db`, alias koneksi `simda`), bukan `sikd_db`.** `config/db_router.py::SimdaRouter.allow_relation()` **menolak** relasi lintas-app ke `simda_dosen`. Akibatnya **`ForeignKey` lintas app dari `presensi` ke `simda_dosen.DataDosen` TIDAK BISA dipakai** (Postgres tidak bisa JOIN/FK lintas database, dan router memang sengaja memblokirnya).

**[MIGRASI KUNCI IDENTITAS — selesai per 2026-07-29]** Kunci identitas presensi sudah **diganti dari `nidn` (CharField) menjadi `user` (ForeignKey ke `accounts.User`)** di semua 6 tabel (`Presensi`, `JadwalKerja`, `Perangkat`, `EnrolmentWajah`, `IzinCuti`, `LogKecurangan`) — supaya presensi mencakup **staf/tendik juga** (staf tidak punya NIDN, jadi tidak bisa terus dikunci NIDN). `accounts.User` ada di database yang SAMA (`sikd_db`) dengan presensi, jadi FK biasa aman dipakai di sini (beda dengan `simda_dosen.DataDosen` yang di database terpisah). Untuk mengayakan data dosen, ambil NIDN lewat `user.nidn` lalu panggil `presensi.utils.get_dosen_by_nidn(user.nidn)` seperti sebelumnya — staf cukup pakai field `accounts.User` langsung (tidak ada equivalent SIMDA untuk staf).

Migrasi database dibuat 3 tahap (lihat `presensi/migrations/0002`–`0004`) supaya aman untuk data produksi yang sudah ada: (1) tambah field `user` nullable, (2) migrasi data — backfill `user` dari pencocokan `nidn` lama ke `accounts.User.nidn`, (3) wajibkan `user` + hapus field `nidn`/`approver` lama — **tahap 3 sengaja dipisah dan HARUS diverifikasi dulu** (lihat docstring di `0003_backfill_user_dari_nidn.py`) sebelum dijalankan, supaya tidak gagal kalau ada baris yang NIDN-nya tidak cocok dengan `accounts.User` mana pun.

Cakupan yang BENAR-BENAR tampil di dashboard/rekap presensi saat ini **masih dosen-only** (lewat `get_dosen_queryset` dari app `laporan`, dipakai di `presensi/views.py`) — skema sudah siap untuk staf, tapi UI/query di lapisan views belum diperluas untuk menampilkan staf. Ini pekerjaan lanjutan yang terpisah (ganti sumber queryset-nya di views.py), bukan perubahan skema lagi.

---

## 3. Model Presensi — Aturan Inti

Presensi diterima **HANYA jika DUA syarat lolos** (gerbang **DAN**):

1. **Cek Lokasi** — koordinat GPS berada di dalam radius geofence `LokasiKantor` (dihitung server-side dengan formula Haversine, lihat `presensi/geo.py` — bukan PostGIS, lihat Bagian 2 & 8).
2. **Verifikasi Wajah** — selfie cocok dengan embedding hasil enrolment **DAN** lolos liveness (bukan foto/video).

**Opsi tambahan (feature flag per lokasi, default MATI):**
- **QR dinamis** — token acak berumur ±60 detik, sekali pakai.
- **Cek Wi-Fi/IP** — perangkat terhubung ke SSID/IP jaringan kantor.

Bila flag opsi menyala, ia menjadi sinyal tambahan pada mesin keputusan. Rancang mesin keputusan **modular** agar syarat bisa dicolok/dicabut lewat konfigurasi, bukan hardcode.

### Mesin skor risiko
Setiap presensi diberi `skor_risiko` dari gabungan sinyal (lokasi, wajah, perangkat, opsi). Keputusan:
- **Risiko rendah** → otomatis sah
- **Risiko sedang** → ditandai (`ditandai=True`) untuk tinjauan HR
- **Risiko tinggi** → ditolak + dicatat di `LogKecurangan`

**[STATUS implementasi gerbang, per 2026-07-28]** Gerbang-DAN (lokasi + wajah) sudah aktif penuh (`presensi/decision.py`, `presensi/views.py`), tapi saat ini masih **biner**: lolos KEDUA syarat → langsung risiko rendah (otomatis sah, `ditandai=False`); gagal salah satu → langsung ditolak (tidak ada baris Presensi dibuat, cuma tercatat `LogKecurangan`). Tingkat **sedang** (ditandai untuk tinjauan HR) belum benar-benar dipakai untuk presensi baru — kalau nanti dibutuhkan nuansa (mis. skor kemiripan wajah pas-pasan di atas ambang tetap ditandai, bukan langsung disahkan), itu perbaikan lanjutan di skor_risiko, belum diterapkan. Liveness saat ini **BARU heuristik sederhana** (1 wajah terdeteksi, skor deteksi cukup, ukuran wajah wajar) — **BUKAN** anti-spoof sungguhan; lihat `presensi/face.py` untuk detail & catatan upgrade.

---

## 4. Prinsip Keamanan (WAJIB dipatuhi di semua kode)

- **Server adalah otoritas.** Klien hanya mengirim data mentah (koordinat, foto, token). **Semua keputusan valid/tidak dihitung di server**, jangan pernah percaya flag "valid" dari klien.
- **Waktu dari server.** Timestamp absen memakai waktu server, bukan waktu perangkat.
- **Rahasia tidak boleh di kode.** Semua kredensial (password DB, `SECRET_KEY`, kunci API) lewat variabel lingkungan / file `.env`. Pastikan `.env` ada di `.gitignore`. **Jangan pernah commit rahasia.**
- **Data biometrik = data pribadi spesifik (UU PDP No. 27/2022).** Simpan **face embedding terenkripsi**, bukan foto mentah bila memungkinkan; batasi retensi; sediakan jalur penghapusan.
- **Semua endpoint butuh autentikasi** (JWT) + **rate-limiting** + validasi input.
- **HTTPS wajib** (kamera & GPS di browser hanya aktif di koneksi aman).

---

## 5. Konvensi Kode

- Ikuti gaya & struktur yang sudah ada di portal kinerja (lihat hasil audit). Konsistensi lebih penting daripada preferensi pribadi.
- **Komentar dan pesan untuk pengguna ditulis dalam Bahasa Indonesia.** Nama variabel/fungsi boleh Inggris standar.
- **Setiap fitur wajib disertai unit test.** Sertakan kasus normal DAN kasus kecurangan (di luar radius, wajah tidak cocok, token kedaluwarsa).
- Proses berat (pencocokan wajah, enrolment) dijalankan asinkron via Celery bila memungkinkan.
- Modul presensi berada di Django app tersendiri bernama `presensi`.
- Migrasi database selalu disertakan dan diperiksa sebelum dijalankan.

---

## 6. Alur Kerja & Aturan untuk Claude Code

- **Kerja per potongan kecil.** Satu fitur per kali; jangan menulis banyak fitur sekaligus dalam satu perubahan besar.
- **Selalu jelaskan rencana singkat sebelum menulis kode**, lalu tulis kode + test.
- **Jangan menjalankan perintah destruktif** (drop database, hapus migrasi, `git push --force`, hapus file massal) tanpa konfirmasi eksplisit.
- **Jangan mengubah file di luar cakupan tugas** yang diminta.
- Bila asumsi tentang struktur portal kinerja belum pasti, **tanya atau periksa dulu** — jangan menebak.
- Setelah membuat fitur, **ingatkan untuk menjalankan test dan meninjau diff** sebelum commit.

---

## 7. Perintah Penting

```bash
# Aktifkan venv proyek dulu (JANGAN pakai python sistem — versi Django beda)
venv\Scripts\activate

# Migrasi database
python manage.py makemigrations
python manage.py migrate

# Menjalankan test modul presensi
python manage.py test presensi

# Menjalankan server pengembangan
python manage.py runserver

# Worker Celery (proses asinkron) — Celery/Redis BELUM terpasang, lihat catatan Bagian 8
celery -A config worker -l info
```
Nama proyek Django (untuk `-A`): **`config`** (lihat `config/settings.py`, `config/wsgi.py`).

---

## 8. Langkah Pertama (sekali di awal)

Sebelum menulis kode fitur, minta Claude Code:
1. Memetakan struktur repositori: versi Django/Python, daftar app, model pegawai, cara autentikasi, framework frontend.
2. Mengisi semua bagian **[ISI SETELAH AUDIT]** di file ini.
3. Mengonfirmasi bahwa PostGIS bisa diaktifkan di database.

Baru setelah itu lanjut ke Tahap 2 (Model & DB) pada panduan coding.

**[HASIL AUDIT — status PostGIS/GDAL, per 2026-07-28]**
- **Belum bisa dikonfirmasi aktif**, dan **keputusan: tidak dipakai untuk sekarang.** Service `postgresql-x64-18` di mesin lokal berstatus **Stopped** (Claude Code tidak bisa start service Windows tanpa hak admin), dan percobaan koneksi (lewat Django maupun `psql` langsung) memakai kredensial di `.env` gagal (`password authentication failed for user "postgres"`) — perlu dicek langsung oleh yang punya akses DB. Mengimpor `django.contrib.gis` juga gagal (`Could not find the GDAL library` — GDAL native library tidak terpasang di mesin Windows ini).
- **Karena itu presensi TIDAK memakai PostGIS/GDAL/`PointField`.** Sebagai gantinya: koordinat disimpan sebagai `latitude`/`longitude` (`FloatField`) biasa, jarak ke `LokasiKantor` dihitung dengan formula **Haversine** murni-Python di `presensi/geo.py`. Ini menghindari ketergantungan pada GDAL sepenuhnya (baik di dev maupun deploy) dan cukup akurat untuk geofence berskala ratusan meter — cocok untuk MVP dan untuk yang baru pertama kali membuat model bergeo-lokasi.
- App `presensi` **sudah** dijadikan app Django lengkap dan didaftarkan di `INSTALLED_APPS` (`__init__.py`, `apps.py`, model diperbaiki, `admin.py`, `tests.py`, migrasi awal `0001_initial` sudah dibuat — lihat `presensi/migrations/`). **Migrasi belum dijalankan** (`python manage.py migrate`) karena Postgres lokal tidak bisa diakses (lihat poin di atas) — jalankan sendiri setelah DB lokal aktif.
- Paket lain yang disebut di CLAUDE.md Bagian 2 tapi **belum ada di `requirements.txt`**: `djangorestframework`, `djangorestframework-simplejwt` (atau setara), `celery`, `redis`, `insightface`/`onnxruntime` (face recognition), library encryption field (mis. `django-cryptography` — `cryptography` sendiri sudah terpasang jadi Fernet manual juga bisa). Semua ini perlu ditambahkan bertahap sesuai fitur yang dikerjakan (Tahap 3 dst).
- Yang **sudah** cocok dengan kebutuhan presensi: `psycopg2-binary` ✓, `django-ratelimit` ✓ (rate-limiting), `django-axes` ✓ (lockout, bisa dipakai pola serupa untuk device/percobaan presensi), `cryptography` ✓ (untuk enkripsi embedding).

---

## 9. Urutan Pengembangan (ringkas)

Model & DB → API + Cek Lokasi (syarat 1) → Verifikasi Wajah (syarat 2) → Gerbang 2 syarat + skor risiko → Opsi QR & Wi-Fi (feature flag) → PWA (alur 2 langkah) → Dashboard admin/HR → Test, keamanan, deploy.

**MVP dulu:** absen dengan cek lokasi saja agar cepat bisa dicoba, baru tambahkan verifikasi wajah.

**[STATUS per 2026-07-28]** Model & DB ✅, API + Cek Lokasi ✅ (sudah dites live di `kinerja.unisan-g.id`, endpoint `POST /api/presensi/masuk|pulang`, `GET /api/presensi/status-hari-ini`, JWT via `djangorestframework-simplejwt`), halaman web dasar untuk coba-coba ✅ (`/presensi/`, `templates/presensi/absen.html` — bukan PWA installable, cuma halaman biasa dalam portal, dipanggil lewat sesi Django), dashboard tinjauan HR/admin ✅ (`/presensi/tinjau/`, `templates/presensi/tinjau.html` — daftar presensi `ditandai=True`, tombol Setujui/Tolak, dibatasi ke role `admin` + `dekan/wadek/kaprodi/sekprodi/operator` dan di-scope per fakultas/prodi lewat `simda_dosen.utils.dapat_kelola_nidn`, sama seperti pola `profil`), Verifikasi Wajah ✅ (syarat 2 — `presensi/face.py`, InsightFace `buffalo_l` di CPU, embedding terenkripsi Fernet, endpoint `POST /api/presensi/enrolment-wajah` + halaman `/presensi/enrolment/`; gerbang-DAN penuh aktif di `masuk`/`pulang`: lolos keduanya → otomatis `rendah`, gagal salah satu → ditolak + `LogKecurangan`, lihat catatan biner/liveness-heuristik di Bagian 3), PWA installable ✅ (`static/presensi-manifest.json`, service worker minimal disajikan di `/presensi/sw.js` — sengaja bukan lewat static/WhiteNoise, supaya cakupannya otomatis `/presensi/*` tanpa header `Service-Worker-Allowed` tambahan di Nginx; ikon dari logo kampus yang sudah ada, `static/img/kampus.png`; tanpa caching offline berat karena GPS/kamera tetap butuh live), Dashboard admin ✅ (`/presensi/dashboard/` — KPI hari ini, tren 6 hari via Chart.js, keterlambatan teratas; `/presensi/data/` — tabel harian 1-baris-per-dosen termasuk yang belum absen, filter fakultas/prodi; ekspor Excel di `/presensi/data/ekspor/` pakai gaya/pola yang sama dengan app `laporan`; logika ada di `presensi/rekap.py`, dipakai ulang oleh view & ekspor).

**[KEPUTUSAN & CATATAN PENTING per 2026-07-28, dari diskusi mockup UI]**
- **Portal TETAP menempel di SIKD**, tidak dipisah jadi aplikasi/domain sendiri — sudah diputuskan setelah menimbang mockup terpisah, alasan lengkap ada di riwayat percakapan (reuse auth/SSO/SIMDA-integration yang sudah ada, mockup itu sendiri berasumsi domain yang sama).
- ✅ **Fondasi staf SELESAI per 2026-07-29** — skema presensi sudah dikunci `user` (`ForeignKey` ke `accounts.User`), bukan `nidn` lagi, lihat detail migrasi di Bagian 2.1. Yang MASIH `dosen-only`: dashboard (`/presensi/dashboard/`) dan tabel data (`/presensi/data/`) di `presensi/views.py` masih memanggil `get_dosen_queryset` (dosen-only) dari app `laporan` — perluasan ke staf tinggal ganti sumber queryset user-nya di sana, TIDAK perlu ubah skema/model lagi.
- ⚠️ **Deteksi mock-GPS/fake-location sungguhan TIDAK BISA dipenuhi lewat PWA/web** (browser tidak pernah mengekspos status mock-location ke JavaScript) — butuh aplikasi native kalau ini jadi kebutuhan wajib. User sudah konfirmasi **belum wajib** sekarang, PWA/web diterima apa adanya untuk tahap ini.
- ✅ **Pengajuan Izin/Sakit/Dinas mandiri SELESAI per 2026-07-29** — `/presensi/izin/` (form pengajuan + riwayat pengajuan pribadi) dan `/presensi/izin/tinjau/` (persetujuan atasan, scoping sama seperti Tinjau Presensi lewat `accounts.User.dapat_kelola`). `IzinCuti.approver` sudah jadi FK ke `accounts.User` (bukan lagi CharField "NIDN/ID atasan").
- ✅ **Kelompok Presensi & Jam Kerja (Dosen 08.00-14.00 Senin-Sabtu vs Pejabat 08.00-16.00) SELESAI per 2026-07-29** — model `KelompokPresensi` (`nama`, `roles` ArrayField, `hari_kerja` ArrayField, `jam_masuk`, `jam_pulang`, `toleransi_menit`, `aktif`) dan `HariLibur` (`tanggal` unik, `keterangan`, `jenis`) ditambahkan di `presensi/models.py`, migrasi `0005_harilibur_kelompokpresensi_presensi_kelompok` + data awal `0006_seed_kelompok_presensi` (seed "Dosen" 08.00-14.00 roles=`["dosen"]` & "Pejabat" 08.00-16.00 roles=`["dekan","wadek","kaprodi","sekprodi","rektorat","biro"]`, keduanya Senin-Sabtu). Perlu `'django.contrib.postgres'` ditambahkan ke `INSTALLED_APPS` (`config/settings.py`) supaya `ArrayField` bisa dipakai (`postgres.E005`). Mesin keputusan (`presensi/decision.py::resolve_kelompok(user)` + `tentukan_status_waktu(lokasi, jam, kelompok=None, tanggal=None)`) memetakan role akun ke kelompok otomatis, memprioritaskan jam kelompok di atas jam `LokasiKantor` (fallback kalau role belum dipetakan), dan mengembalikan `HADIR` tanpa hitungan telat untuk `HariLibur` maupun hari di luar `hari_kerja` kelompok (mis. Minggu). `AbsenMasukView` (`presensi/views.py`) sudah men-snapshot `kelompok` yang berlaku ke `Presensi.kelompok` saat absen masuk (bukan FK live ke role saat ini — lihat docstring `KelompokPresensi`, penting untuk akurasi dasar penggajian kalau jabatan berubah nanti). Admin diregistrasi (`KelompokPresensiAdmin`, `HariLiburAdmin`) + test ditambahkan (`ResolveKelompokTest`, `TentukanStatusWaktuTest`, `SeedKelompokPresensiTest`, plus 1 test snapshot di `AbsenMasukAPITest`) — **belum dijalankan** (Postgres lokal masih tidak bisa diakses, lihat Bagian 8), perlu dites di VPS setelah deploy. Lokasi laporan rekap bulanan (app `laporan` vs halaman baru di `presensi`) **masih belum diputuskan**, ditunda ke pembahasan berikutnya.

**[POLESAN UI MOBILE — Tingkat 1 selesai per 2026-07-29]** Terinspirasi mockup UI yang diberikan user (bukan dikerjakan sama persis — beberapa elemen mockup SENGAJA tidak ditiru, lihat alasan di bawah). Yang sudah ditambahkan ke `templates/presensi/absen.html`: header sapaan personal (nama+inisial avatar+pill role/fakultas, dihitung di `presensi/views.py::halaman_absen`), jam digital + tanggal berjalan (live, JS), alur 2-langkah dengan indikator progres (Lokasi → Wajah — BUKAN 3 langkah termasuk QR seperti mockup, karena QR belum diimplementasikan), visual lingkaran geofence dekoratif (CSS, bukan peta sungguhan), kartu "Berhasil" dengan ringkasan (waktu/lokasi/status/tingkat risiko) menggantikan alert biasa.

**Sengaja TIDAK ditiru dari mockup** (lihat analisis lengkap di riwayat percakapan): (1) chip "Lokasi palsu (mock): Tidak terdeteksi" — PWA/web tidak bisa membuktikan klaim itu jujur (browser tidak expose status mock-location ke JS), jadi tidak ditampilkan supaya tidak menyesatkan pengguna; (2) ringkasan bulanan Hadir/Telat/Izin/Alpa di beranda — Izin & Alpa belum bisa dihitung akurat (logika alpa belum ada), ditunda.

**[TINGKAT 2 UI MOBILE — selesai per 2026-07-29]** Tombol Absen Pulang dibedakan warnanya (hijau) dari Absen Masuk (biru). Bottom navigation bar (`templates/presensi/_bottom_nav.html`, `d-md-none` — cuma tampil di lebar mobile) ditambahkan ke `absen.html`/`riwayat.html`/`izin.html`: Beranda · Riwayat · Izin · Profil. Halaman **Riwayat Presensi** (`/presensi/riwayat/`) menampilkan gabungan Presensi + IzinCuti disetujui milik sendiri, bulan berjalan. Nav sidebar desktop untuk Presensi/Riwayat/Izin & Cuti diperluas ke **SEMUA role** (bukan cuma dosen lagi), karena halaman-halaman ini sudah generik berbasis `user` (siap dipakai staf juga, bukan tergantung NIDN).

**[KELOMPOK STAF/TENDIK, TARGET JAM KERJA BULANAN, & LEMBUR — selesai per 2026-07-29]** Perluasan dari fitur Kelompok Presensi di atas, atas permintaan user untuk melengkapi laporan presensi sebagai dasar penggajian:
- ✅ **Kelompok "Staf/Tendik"** ditambahkan (migrasi data `0008_seed_kelompok_staf_tendik`) — baris kelompok TERPISAH dari "Pejabat" (bukan digabung), jam kerja sama persis (08.00-16.00 Senin-Sabtu) tapi role `["tendik"]` sendiri. Keputusan user: dipisah supaya jam/target tendik bisa diatur beda dari pejabat nanti tanpa migrasi skema lagi.
- ✅ **Model `TargetKerjaBulanan`** (`presensi/models.py`, migrasi `0007_targetkerjabulanan`) — `kelompok` (FK), `bulan`, `tahun`, `target_hari_kerja`, `target_jam_kerja`. Diisi **MANUAL per bulan+tahun** oleh HR lewat admin (bukan dihitung otomatis dari kalender) — keputusan user, supaya HR bisa atur beda tiap bulan (mis. Ramadan/semester pendek) dan tetap pegang kendali penuh atas angka resmi dasar penggajian.
- ✅ **Status kedatangan/kepulangan granular** — `Presensi` (migrasi `0009_tambah_field_lembur_presensi`) dapat field baru: `menit_lebih_awal`, `menit_terlambat` (dihitung saat masuk), `menit_pulang_cepat`, `menit_lembur` (dihitung saat pulang) — SEMUA selisih murni dari jam kelompok/lokasi, TIDAK memperhitungkan `toleransi_menit` (itu cuma untuk status HADIR/TELAT biner di `tentukan_status_waktu`). Dihitung di `presensi/decision.py::hitung_ketepatan_masuk/pulang`, dipanggil dari `AbsenMasukView`/`AbsenPulangView`. Field `status` (HADIR/TELAT/IZIN/ALPA/DITOLAK) TIDAK diubah/digantikan — field granular ini melengkapi, bukan menggantikan.
- ✅ **Alur lembur & approval** — lembur > `BATAS_MENIT_LEMBUR_WAJIB_KETERANGAN` (120 menit, `presensi/models.py`) WAJIB diisi `keterangan_lembur` sebelum `AbsenPulangView` menerima absen pulang (alasan `keterangan_lembur_wajib` kalau kosong — user harus isi field lalu ulangi, lihat textarea kondisional di `absen.html`); lembur ≤120 menit otomatis diterima tanpa keterangan/approval. Kalau >120 menit & keterangan terisi, `status_lembur` jadi `menunggu` dan tampil di kartu baru "Lembur Menunggu Persetujuan" di `/presensi/tinjau/` (`templates/presensi/tinjau.html`), diputuskan lewat `putuskan_lembur` (`presensi/views.py`, URL `tinjau/<id>/putuskan-lembur/`) — approve/reject pola sama seperti `putuskan_izin`. **Kalau ditolak, `waktu_pulang` ASLI TIDAK diubah** (tetap sesuai realita, prinsip "server adalah otoritas") — yang dibatasi cuma jam kerja TERHITUNG (lihat poin berikut).
- ✅ **`Presensi.durasi_kerja_menit` / `.durasi_kerja`** (properti, `presensi/models.py`) — durasi kerja efektif dalam menit / format "JJ:MM" (`format_jam_menit()`). Kalau `status_lembur` `menunggu` atau `ditolak`, jam pulang efektif yang dipakai untuk hitung durasi dibatasi ke jam pulang normal kelompok/lokasi (jam lembur tidak ikut dihitung sampai disetujui). Ditampilkan per-baris di `/presensi/riwayat/` (`templates/presensi/riwayat.html`).
- ✅ **`presensi/rekap.py::rekap_bulanan_user(user, bulan, tahun)`** — jumlah `durasi_kerja_menit` sebulan (presensi `DITOLAK` dikecualikan) dibandingkan ke `TargetKerjaBulanan` kelompoknya (kelompok diambil dari snapshot presensi bulan itu). Ditampilkan sebagai ringkasan "Total jam kerja bulan ini vs Target" di bagian atas `/presensi/riwayat/`.
- ✅ **Laporan Bulanan Jam Kerja lintas-pegawai SELESAI per 2026-07-29** — `/presensi/laporan-bulanan/` (`templates/presensi/laporan_bulanan.html`) & ekspor Excel `/presensi/laporan-bulanan/ekspor/`, ditaruh di app `presensi` (BUKAN app `laporan` — app itu isinya laporan kinerja riset/publikasi, domain berbeda). **Sengaja LINTAS-PEGAWAI** (dosen + staf/tendik + pejabat via `accounts.User` langsung, BUKAN `get_dosen_queryset` yang dosen-only) — cakupannya di-scope lewat `dapat_kelola`, pola sama dengan `/presensi/tinjau/`. Logika `presensi/rekap.py::laporan_bulanan_jam_kerja(user_qs, bulan, tahun)` — 1 baris per user yang PUNYA presensi di bulan itu (bukan semua user dalam cakupan seperti `data_presensi_harian`), pakai `rekap_bulanan_user` per baris + `selisih_menit`/`selisih_jam_kerja` (format "+JJ:MM"/"-JJ:MM" vs target). Link nav ditambahkan di `templates/base.html` section Administrasi (role sama dengan Tinjau Presensi). Ini menjawab catatan lama soal lokasi laporan rekap bulanan yang "masih belum diputuskan" — sudah diputuskan & dibangun di app `presensi`.
- Test baru: `FormatJamMenitTest`, `HitungKetepatanMasukPulangTest`, `DurasiKerjaPresensiTest`, `SeedKelompokStafTendikTest`, `RekapBulananUserTest`, `PutuskanLemburViewTest`, `LaporanBulananViewTest`, plus kasus lembur di `AbsenPulangAPITest` — **sudah dites & LOLOS di VPS per 2026-07-29 (114/114 test saat itu)**, setelah 2 ronde perbaikan bug test murni (bukan bug aplikasi): (1) `timezone.localdate()` yang dipanggil di dalam body test yang `timezone.now`-nya sudah di-`@patch` ikut mengembalikan `MagicMock` — perbaikannya hitung tanggal SEBELUM patch aktif (di `setUp`); (2) perbandingan `waktu_pulang.time()` setelah `refresh_from_db()` harus lewat `timezone.localtime()` dulu (ORM mengembalikan datetime UTC, bukan waktu lokal WITA yang aslinya di-set); (3) `test_pulang_setelah_masuk_diterima` sempat jadi flaky tergantung jam sungguhan server (bisa kena aturan wajib `keterangan_lembur` kalau kebetulan dites >2 jam lewat jam pulang kelompok) — diperbaiki dengan mem-pin waktu lewat mock, bukan real clock.

**[UI PENGATURAN & PELENGKAP TAMPILAN — selesai per 2026-07-29]** Setelah fitur di atas dibangun, user menanyakan apakah semua pengaturan (kelompok, hari libur, target) sudah punya tampilan di portal atau baru di database/Django Admin saja -- jawabannya saat itu SEBAGIAN (kelompok/hari-libur/target cuma bisa diatur lewat `/admin/`, Dashboard & Data Presensi belum menampilkan kelompok/lembur). Ini semua sudah dilengkapi:
- ✅ **Halaman Pengaturan Presensi** (admin-only, BUKAN di-scope fakultas/prodi seperti Tinjau Presensi — institusi-wide jadi sengaja dibatasi role `admin` saja lewat `_bisa_kelola_pengaturan_presensi`): `/presensi/pengaturan/kelompok/`, `/pengaturan/hari-libur/`, `/pengaturan/target/` (+ form tambah/ubah masing-masing). Kelompok pakai **toggle aktif/nonaktif** (`toggle_aktif_kelompok`), BUKAN hapus — `KelompokPresensi` direferensikan `Presensi.kelompok` dengan `on_delete=PROTECT` jadi tidak boleh dihapus kalau sudah pernah dipakai. `KelompokPresensiForm.roles`/`hari_kerja` pakai checkbox (`CheckboxSelectMultiple`), bukan input teks dipisah koma, supaya HR non-teknis lebih mudah pakainya. Link nav "Pengaturan Presensi" ditambahkan di `templates/base.html` section Administrasi (gated `role == 'admin'`, sama seperti Kelola User/Data Master).
- ✅ **`TargetKerjaBulanan.nama_bulan`** (properti baru, `presensi/models.py`) — pengganti `get_bulan_display()` yang tidak ada karena field `bulan` sengaja `PositiveSmallIntegerField` biasa tanpa `choices` (supaya tidak perlu migrasi kalau nanti mau ganti cara render). `NAMA_BULAN` (list nama bulan Indonesia) juga di `models.py`, dipakai ulang oleh `TargetKerjaBulananForm.bulan` (`forms.py`) supaya tidak duplikasi.
- ✅ **Data Presensi Harian** (`/presensi/data/` + ekspor Excel) — kolom **Kelompok** & **Lembur** (badge menunggu/disetujui/ditolak) ditambahkan, di halaman maupun file Excel-nya (kolom Excel jadi A-J, sebelumnya A-H).
- ✅ **`top_telat_hari_ini`** (`presensi/rekap.py`) — diperbaiki supaya pakai `Presensi.menit_terlambat` yang SUDAH tersimpan (dihitung `hitung_ketepatan_masuk`, kelompok-aware) alih-alih menghitung ulang dari `lokasi.jam_masuk` saja seperti sebelumnya (yang mengabaikan prioritas jam kelompok).
- ✅ **Riwayat Presensi** — label granular "Terlambat N menit" / "Datang N menit lebih awal" / "Pulang N menit lebih cepat" ditambahkan per baris (dari field `menit_terlambat`/`menit_lebih_awal`/`menit_pulang_cepat` yang sebelumnya tersimpan tapi tidak ditampilkan di mana pun).
- Test baru: `PengaturanKelompokViewTest`, `PengaturanHariLiburViewTest`, `PengaturanTargetViewTest`, `TopTelatMenitTerlambatTest` — **sudah dites & LOLOS di VPS (128/128 test)**.

**[LAPORAN DAFTAR HADIR DOSEN SERDOS (LLDIKTI) — selesai per 2026-07-30]** Presensi punya DUA tujuan: internal kampus, dan pelaporan ke LLDIKTI untuk dosen yang lulus Sertifikasi Dosen (Serdos) — laporan resminya ("Daftar Hadir Dosen Tetap Yayasan Penerima Serdos") butuh paraf per hari kerja. User memberi contoh dokumen fisik (foto) sebagai acuan format persis.
- ✅ **Paraf Digital** (`presensi/models.py::ParafDosen`, halaman `/presensi/paraf/`) — dosen gambar paraf SEKALI lewat canvas (Pointer Events API, mouse/jari), disimpan sebagai gambar biasa (bukan biometrik, tidak perlu enkripsi Fernet), lalu **auto-stamp** dipakai berulang ke laporan (keputusan user: sederhana & sesuai permintaan, meski nilai pembuktian sebagai "tanda tangan asli tiap hari" lebih lemah dari kertas — sudah didiskusikan & diterima). Endpoint `POST /api/presensi/paraf`. Link nav khusus role `dosen`.
- ✅ **Cakupan: HANYA dosen serdos DISETUJUI** — `presensi/utils.py::get_dosen_serdos_qs()` filter `profil.Sertifikasi(jenis_sertifikasi='serdos', status_validasi='disetujui')`. Sengaja lebih ketat dari beberapa tempat lain di kode (`profil/views.py::has_serdos` dkk cuma exists() tanpa filter status) — untuk laporan resmi ke lembaga eksternal, cuma yang sudah tervalidasi yang relevan.
- ✅ **Data pendukung SIMDA baru** (`simda_dosen/models.py`, read-only managed=False, pola sama seperti `DataDosen`): `JabatanStruktural` (`master.jabatan_struktural`) & `PejabatStruktural` (`master.pejabat_struktural`, punya `file_ttd`/`lebar_ttd`/`tinggi_ttd` — SIMDA SUDAH punya infrastruktur gambar tanda tangan resmi untuk dokumen PDF). Helper `simda_dosen/utils.py::get_pejabat_aktif(nama_jabatan)` cari pejabat aktif (mis. "Rektor") buat blok tanda tangan otomatis. **PENTING — perlu langkah manual di database SIMDA**: tabel ini belum ada di grant `sikd_rw` (`buat_role_sikd_rw.sql`) — sudah dibuatkan `tambah_akses_pejabat_struktural.sql` (repo SIMDA, `C:\unisan\simda\`, TIDAK masuk repo kinerja-dosen karena beda proyek) dan dikirim ke user untuk dijalankan sendiri di database `unisan_db` lewat pgAdmin/psql. Tanpa grant ini, `get_pejabat_aktif` akan gagal query (permission denied) di produksi.
- ✅ **Urutan Laporan Serdos** (`presensi/models.py::UrutanSerdos`, tab baru "Urutan Laporan Serdos" di Pengaturan Presensi) — urutan baris di laporan resmi mengikuti urutan SK/kepegawaian institusi (BUKAN alfabetis nama/NIDN, sesuai contoh dokumen), yang tidak tersimpan di data mana pun (SIKD maupun SIMDA) — jadi diatur manual admin lewat satu form besar (bukan tambah/ubah satu-satu, supaya HR bisa isi semua dosen serdos sekaligus). Dosen yang belum diatur urutannya ditaruh di akhir daftar, urut nama.
- ✅ **`presensi/laporan_serdos.py::data_laporan_serdos(bulan, tahun)`** — logika inti (dipisah dari `views.py`/`rekap.py`, domainnya beda: kepatuhan LLDIKTI, bukan payroll internal), return list `BarisSerdos` per dosen: grid tanggal (`HariGrid`: kerja/minggu/libur + status hadir), Gol/Jabatan Akademik dirakit dari `GolonganPublik.kode` + `JabatanFungsionalPublik.nama` SIMDA (DataDosen cuma simpan id-nya, bukan teksnya), deteksi "Tugas Belajar" dari `DataDosen.status_kepegawaian_nama` (field ini SUDAH ada di SIMDA, dipetakan otomatis — baris dosen tugas belajar diganti teks "TUGAS BELAJAR / BIAYA PEMERINTAH" merentang, bukan grid paraf).
- ✅ **Ekspor PDF** (`presensi/laporan_serdos_pdf.py`, reportlab — library pertama di project yang menyisipkan GAMBAR ke PDF, sebelumnya cuma tabel teks) — page size A3 landscape (dibutuhkan supaya ±31 kolom tanggal muat), replikasi header (logo+judul+yayasan/PTS/bulan), tabel dengan gambar paraf ditempel per sel, baris Tugas Belajar pakai `SPAN`, kolom Minggu/hari libur diarsir **abu solid** (BUKAN pola diagonal seperti kertas asli — reportlab Table tidak punya hatch-fill bawaan, dan bikin custom canvas draw per sel jauh lebih rumit untuk hasil yang secara fungsi sama), legenda KET, blok tanda tangan (gambar TTD dari SIMDA + nama digarisbawahi + NIP).
- ✅ **Ekspor Excel** (`presensi/laporan_serdos_excel.py`, openpyxl) — versi sama persis, gambar ditempel pakai `openpyxl.drawing.image.Image` (catatan: nempel sebagai overlay ke sel, BUKAN "fit di dalam" sel seperti reportlab Table cell — keterbatasan openpyxl yang sudah diketahui, bukan bug).
- ✅ **Halaman `/presensi/laporan-serdos/`** (admin-only) — form bulan/tahun/kota/tanggal cetak/jabatan+nama+NIP penandatangan (nama/NIP/gambar TTD **otomatis** dari `get_pejabat_aktif` kalau field dikosongkan, tapi tetap bisa diedit manual — mis. kalau mau ditandatangani pejabat lain). Tanggal cetak default = tanggal 1 bulan BERIKUTNYA (pola dari contoh dokumen: laporan bulan berjalan ditandatangani awal bulan depan). Satu form, dua tombol unduh (PDF/Excel) pakai `formaction` HTML — bukan dua form terpisah.
- Test baru: `GetDosenSerdosQsTest`, `JenisTanggalBulanTest`, `DataLaporanSerdosTest`, `GetPejabatAktifTest`, `PengaturanUrutanSerdosViewTest`, `LaporanSerdosViewTest` — SIMDA di-mock (pola sama dengan `GetDosenByNidnTest`, database `simda` tidak selalu tersedia saat test).
- ⚠️ **Bug ditemukan & diperbaiki setelah deploy pertama**: `get_pejabat_aktif` belum membungkus query `master.pejabat_struktural` dengan penanganan error — sebelum user sempat menjalankan `tambah_akses_pejabat_struktural.sql` di SIMDA, `ProgrammingError: permission denied for table pejabat_struktural` bikin **seluruh** halaman `/presensi/laporan-serdos/` 500 (bukan cuma field nama/NIP kosong seperti yang diklaim sebelumnya). Diperbaiki dengan `try/except DatabaseError: return None` di `simda_dosen/utils.py::get_pejabat_aktif` — aman ditangkap tanpa rollback manual karena koneksi `'simda'` jalan di mode autocommit (tidak ada `ATOMIC_REQUESTS` di settings). Test regresi: `GetPejabatAktifTest::test_permission_denied_mengembalikan_none_bukan_crash`.

**[LAPORAN PRESENSI INTERNAL — selesai per 2026-07-31]** Beda dari Laporan Daftar Hadir Serdos (format resmi LLDIKTI, dosen serdos saja, ada paraf): ini laporan bulanan **internal kampus**, lintas-pegawai (dosen+pejabat+tendik), tanpa paraf — isinya jam Masuk/Pulang harian. Bisa difilter kategori pegawai, fakultas, program studi.
- ✅ **Kategori — cuma 2: "Dosen" (termasuk Pejabat Struktural) & "Tendik"** — `presensi/laporan_internal.py::KATEGORI_ROLES` = `{"dosen": ["dosen","dekan","wadek","kaprodi","sekprodi","rektorat","biro"], "tendik": ["tendik"]}`. **Keputusan per 2026-07-31** (user awalnya tanya apakah jam kerja presensi Dosen & Pejabat sebaiknya disatukan — dianalisis & diputuskan TIDAK, KelompokPresensi/jam kerja gerbang presensi & Target Kerja Bulanan tetap terpisah seperti semula karena pejabat struktural punya kebutuhan jam kantor lebih panjang untuk tugas administratif; yang digabung CUMA kategori tampilan di laporan ini, karena secara kepegawaian pejabat struktural tetap dosen). Kategori "Pejabat" terpisah yang sempat ada di versi awal (per 2026-07-31 pagi) sudah dihapus. Role `admin`/`operator` sengaja tidak masuk kategori manapun (konsisten dengan seed `KelompokPresensi`) — tetap muncul kalau filter kategori dikosongkan.
- ✅ **Program Studi** — `master.models.Prodi` (BUKAN `simda_dosen.ProdiPublik`/SIMDA — itu untuk keperluan lain, dropdown Mata Kuliah/Mahasiswa). `accounts.User.kode_prodi` cuma CharField kode, jadi nama diambil lewat map `{kode_prodi: nama_prodi}` sekali query (bukan N+1 per user).
- ✅ **Kolom Masuk/Pulang ditumpuk 1 kolom per tanggal** (BUKAN 2 kolom terpisah seperti diminta awalnya) — setelah dianalisis, 2 kolom/tanggal x ±31 hari = ±62 kolom butuh lebar ±43-45cm (lebih lebar dari A3/42cm, apalagi F4/33cm yang diminta user). User pilih opsi ditumpuk (1 kolom/tanggal, teks 2 baris "08:05"/"14:12") supaya tetap muat di F4 landscape — jumlah kolom jadi sama seperti Laporan Serdos (~31).
- ✅ **Ekspor PDF** (`presensi/laporan_internal_pdf.py`, reportlab, **F4 landscape** — beda dari Serdos yang A3, karena di sini cuma teks bertumpuk bukan gambar paraf jadi jauh lebih hemat lebar) & **Excel** (`presensi/laporan_internal_excel.py`, openpyxl) — TANPA paraf/tugas-belajar/blok tanda tangan (laporan internal, bukan dokumen resmi eksternal).
- ✅ **Halaman `/presensi/laporan-internal/`** — akses **scoped `dapat_kelola`** (BUKAN admin-only seperti Pengaturan/Serdos) — sama seperti Laporan Bulanan Jam Kerja, karena ini tools operasional harian, bukan pengaturan institusi-wide. Dropdown Fakultas/Prodi diisi dinamis dari `master.Fakultas`/`master.Prodi` di `LaporanInternalForm.__init__` (query saat request, bukan saat modul di-import).
- Tidak ada model/migrasi baru — laporan ini murni membaca data yang sudah ada (`Presensi`, `accounts.User`, `master.Prodi`/`Fakultas`).
- Test baru: `GetUserQsLaporanInternalTest`, `DataLaporanInternalTest`, `LaporanInternalViewTest` — SEMUA data lokal (tidak ada SIMDA di sini), jadi tidak perlu mock seperti test-test Serdos — **belum dites di VPS**, perlu dijalankan setelah deploy.

**[DETAIL PRESENSI BULANAN — selesai per 2026-07-31]** Fitur opsional drill-down: lihat rincian presensi SATU orang per hari dalam SATU bulan (dicari lewat nama/username/NIDN), dipakai kalau perlu mengecek riwayat kehadiran seseorang secara spesifik — beda dari 3 laporan lain yang semuanya rekap banyak-orang/1-bulan atau diri-sendiri/bulan-berjalan.
- ✅ **Alur pencarian** (`AskUserQuestion`, user pilih "Tampilkan daftar, pilih salah satu") — `presensi/laporan_detail.py::cari_pegawai(user_qs, kata_kunci)` filter `Q` di `first_name`/`last_name`/`username`/`nidn`. 0 hasil → pesan "tidak ditemukan", 1 hasil → langsung tampil detail, >1 hasil → tabel pilihan dengan link "Lihat Detail" (`?user_id=`).
- ✅ **`detail_presensi_bulanan(user, bulan, tahun)`** — 1 baris per tanggal (pakai `jenis_tanggal_bulan` dari `laporan_serdos.py`, dipakai ulang lintas-laporan), gabung data `Presensi` (status/jam masuk-pulang/keterangan granular telat-pulang cepat-lembur dari field `menit_*` yang sudah ada) dan `IzinCuti` disetujui (fallback kalau tidak ada presensi hari itu) — hari kerja tanpa presensi maupun izin ditandai "Alpa", hari libur/minggu tanpa presensi ditandai "Libur". Rekap total jam kerja/target/selisih pakai `rekap_bulanan_user()` yang sudah ada (`presensi/rekap.py`), tidak ada logika baru untuk itu.
- ✅ **Ekspor PDF** (`presensi/laporan_detail_pdf.py`, reportlab, **A4 portrait** — beda dari 3 laporan lain yang landscape lebar, karena di sini cuma ±31 baris tanggal untuk 1 orang, bukan grid banyak-orang) & **Excel** (`presensi/laporan_detail_excel.py`, openpyxl) — user eksplisit minta opsi unduh juga (bukan cuma tampilan layar).
- ✅ **Halaman `/presensi/laporan-detail/`** — akses **scoped `dapat_kelola`** (BUKAN admin-only), pola sama seperti Laporan Bulanan/Internal karena tools operasional harian. Link cross-referensi "Lihat Detail" ditambahkan di baris tabel `/presensi/laporan-bulanan/` supaya gampang loncat dari rekap-banyak-orang ke detail-satu-orang. Nav sidebar "Detail Presensi Bulanan" ditambahkan di section yang sama dengan Laporan Bulanan/Internal.
- **Keputusan lokasi fitur** (dianalisis sesuai permintaan user "cocok digabung dimana"): dibuat halaman baru sendiri, bukan digabung ke salah satu dari 3 laporan yang sudah ada — karena bentuk interaksinya beda (cari-1-orang+bulan-bebas vs daftar-banyak-orang+1-bulan vs diri-sendiri+bulan-berjalan), tapi logika inti (`rekap_bulanan_user`, `jenis_tanggal_bulan`) dipakai ulang penuh, bukan ditulis ulang.
- Tidak ada model/migrasi baru — murni membaca data yang sudah ada (`Presensi`, `IzinCuti`, `accounts.User`).
- Test baru: `CariPegawaiTest`, `DetailPresensiBulananTest`, `LaporanDetailViewTest` — **belum dites di VPS**, perlu dijalankan setelah deploy.
- ✅ **Blok pengesahan opsional (kota/tanggal cetak/penandatangan) — ditambah 2026-08-01.** User minta bisa membubuhkan blok tanda tangan pengesah di ekspor PDF/Excel (dipakai kalau laporan ini mau dilampirkan sebagai bukti fisik). **Beda dari Laporan Serdos**: SEMUA manual, TIDAK auto-fill dari `get_pejabat_aktif`/Pejabat Struktural SIMDA — keputusan sengaja, karena penandatangan Detail Presensi bisa siapa saja (atasan langsung/Dekan/Kaprodi/HR), bukan cuma Rektor seperti laporan Serdos yang levelnya institusi. Field `id_penandatangan` sengaja **satu CharField bebas** (bukan `nip_penandatangan` terpisah dengan prefiks "NIP." otomatis seperti Serdos) — user eksplisit minta bisa isi apa adanya, mis. `"NIP. 199001012020121001"` ATAU `"NIDN. 0910097601"`, karena penandatangan bisa dosen (NIDN) atau pejabat struktural/tendik (NIP). `presensi/views.py::_parameter_penandatangan_detail(request)` baca 5 GET params (`kota`, `tanggal_cetak`, `jabatan_penandatangan`, `nama_penandatangan`, `id_penandatangan`) dengan fallback aman (`tanggal_cetak` invalid/kosong → `timezone.localdate()`, bukan crash), dipakai bareng oleh halaman & kedua ekspor. Blok tanda tangan di PDF (`laporan_detail_pdf.py`)/Excel (`laporan_detail_excel.py`) HANYA dicetak kalau minimal satu dari jabatan/nama/id diisi — kosongkan semua untuk laporan tanpa pengesahan (perilaku lama tetap jalan, semua kwarg baru opsional dengan default aman). Test baru: `test_admin_bisa_unduh_pdf_dengan_penandatangan`, `test_admin_bisa_unduh_excel_dengan_penandatangan`, `test_tanggal_cetak_tidak_valid_pakai_default_hari_ini` di `LaporanDetailViewTest`.

**[MIRROR DataTendik & PERBAIKAN NAMA LAPORAN - selesai per 2026-08-01]** User melaporkan akun jabatan/administratif generik (mis. akun login `fikom`/`ekonomi`/`biro-sdm`, `first_name`/`last_name`-nya diisi nama jabatan bukan nama orang) ikut tampil di Laporan Presensi Internal/Bulanan Jam Kerja/Detail dengan nama jabatan ("Dekan Fikom"), bukan nama pegawai sungguhan. Analisis (lihat riwayat percakapan): akun ini murni untuk login administratif (dikonfirmasi user), TIDAK dipakai presensi fisik -- jadi baris itu tetap relevan tampil di laporan, yang salah cuma SUMBER NAMA-nya (harus dari SIMDA, bukan first_name/last_name akun login).
- OK `simda_dosen.DataTendik` (model baru, `simda_dosen/models.py`, migrasi `0007_datatendik.py`) -- mirror read-only TENDIK versi `DataDosen`, tapi SENGAJA lewat VIEW tersaring `master.v_tendik_ringkas` (BUKAN tabel mentah `master.data_tendik`) -- keputusan privasi: SIKD bukan CRUD-owner data tendik (beda dengan Dosen yang punya self-service profil), jadi cuma di-mirror field identitas dasar yang relevan untuk laporan (nip, nip_yayasan, nama_lengkap, unit_kerja_id, jabatan, jenis_kepegawaian_id, status_kepegawaian_id, golongan_id, is_active) -- field pribadi sensitif tendik (NIK/alamat/rekening/NPWP/foto/tgl lahir) TIDAK di-mirror sama sekali, prinsip sama dengan master.v_dosen_publik untuk konsumen Portal. PERLU LANGKAH MANUAL DI SIMDA (belum dijalankan user): `C:\unisan\simda\buat_view_tendik_ringkas.sql` (bikin view + grant SELECT ke sikd_rw) -- tanpa ini, resolve nama tendik fallback ke nama akun lokal (tidak crash), bukan gagal total.
- OK `PejabatStruktural.tendik_id` diupgrade jadi FK penuh ke `DataTendik` (sebelumnya IntegerField mentah karena DataTendik belum ada) -- cocok dengan skema asli SIMDA (`C:\unisan\simda\sdm\models.py`) yang memang FK penuh. Migrasi RemoveField/AddField ditambahkan MANUAL (bukan hasil makemigrations) karena autodetector Django tidak melacak perubahan field pada model managed=False yang sudah ada sebelumnya (state bookkeeping saja, tidak ada SQL nyata yang dieksekusi untuk model unmanaged).
- OK `simda_dosen/utils.py::attach_nama_resmi(users)` -- helper bulk (hindari N+1 ke SIMDA) yang menempel `.nama_resmi` ke tiap User: dosen dicocokkan lewat nidn ke DataDosen, tendik lewat nip_yayasan ke DataTendik, fallback ke get_full_name()/username kalau tidak ketemu di SIMDA sama sekali (akun jabatan generik TIDAK PERNAH resolve ke SIMDA karena memang bukan representasi satu NIDN/NIP tertentu -- fallback ini tetap dipakai untuk akun itu, bukan bug). Dibungkus try/except DatabaseError (pola sama dengan get_pejabat_aktif) supaya grant v_tendik_ringkas yang belum jalan tidak bikin laporan 500, cukup fallback ke nama lokal. get_pejabat_aktif() juga di-update select_related untuk ikut tendik.
- OK `simda_dosen/utils.py::get_simda_tendik_or_none(user)` -- versi tendik dari get_simda_dosen_or_none, dicocokkan lewat nip_yayasan (tendik tidak punya NIDN).
- OK Wiring ke 3 laporan: `presensi/laporan_internal.py::data_laporan_internal()`, `presensi/rekap.py::laporan_bulanan_jam_kerja()` (+ ekspor Excel-nya di `views.py::export_excel_laporan_bulanan`), `presensi/laporan_detail.py` (lewat `views.py::_cari_target_user_laporan_detail`, karena cari_pegawai() sendiri harus tetap mengembalikan queryset untuk .count()/.first()) -- semua sekarang pakai user.nama_resmi sebagai nama tampilan (halaman & ekspor PDF/Excel), bukan get_full_name(). Laporan Serdos (`laporan_serdos.py`) TIDAK disentuh -- sudah otomatis terbatas ke dosen serdos disetujui, akun jabatan generik tidak mungkin masuk ke situ.
- Test baru: `GetSimdaTendikOrNoneTest`, `AttachNamaResmiTest` (termasuk kasus akun jabatan generik fallback ke nama akun & kasus DatabaseError fallback) -- belum dites di VPS, perlu dijalankan setelah deploy. Assertion `GetPejabatAktifTest` disesuaikan untuk select_related yang sekarang ikut tendik.
- OK **Akun tanpa NIDN dikecualikan dari laporan lintas-pegawai (ditambah 2026-08-01, terpisah dari nama resmi di atas)** -- setelah nama-nama akun jabatan tetap salah tampil (karena memang tidak ada NIDN untuk dicocokkan), dikonfirmasi user akun ini murni login administratif (TIDAK presensi fisik sendiri). `presensi/views.py::_pengguna_dalam_cakupan()` sekarang exclude user non-tendik yang `nidn` kosong dari cakupan Laporan Internal/Bulanan/Detail (`.exclude(~Q(role="tendik") & (Q(nidn__isnull=True) | Q(nidn="")))`) -- tendik dikecualikan dari aturan exclude ini karena memang tidak pernah punya NIDN tapi tetap pegawai sungguhan. Test baru: `test_akun_tanpa_nidn_dikecualikan_dari_cakupan`, `test_tendik_tanpa_nidn_tetap_masuk_cakupan` di `LaporanInternalViewTest` -- **sudah dites & LOLOS di VPS (195/195 test)**.

**[⚠️ KOREKSI per 2026-08-01, lihat "Kelola Data Tendik" di bawah]** Poin di atas menyebut `DataTendik` "SENGAJA lewat VIEW tersaring `master.v_tendik_ringkas`" -- ini SUDAH BERUBAH. Model itu sekarang mengarah ke **tabel mentah `master.data_tendik`** dengan akses baca-tulis penuh, karena SIKD jadi entry-point CRUD data tendik. Detail lengkap di bagian "Kelola Data Tendik" di bawah.

**[KELOLA DATA TENDIK — CRUD penuh, selesai per 2026-08-01]** User bertanya apakah bisa disiapkan menu "Kelola Data Tendik" di SIKD (CRUD berbasis UI, data tersimpan di SIMDA) meniru pola "Profil Dosen". **Analisis sebelum coding** (lihat riwayat percakapan) menemukan fakta penting yang mengubah rencana awal:
- "Profil Dosen" (`profil/views.py::simpan_profil`) **BUKAN CRUD** -- cuma UPDATE, TIDAK PERNAH membuat baris `DataDosen` baru (selalu mengasumsikan sudah ada, dibuat lewat aplikasi SIMDA sendiri). Tidak ada satu pun kode di SIKD yang men-create `DataDosen`.
- Akun login (`accounts.User`, dibuat lewat Kelola User) dan data SIMDA (`DataDosen`) **sepenuhnya terpisah tanpa penautan otomatis** -- NIDN diketik manual, `audit_nidn` cuma mendeteksi ketidakcocokan setelah fakta, tidak mencegah.
- Karena tendik **belum punya satu pun akun login** (dicek langsung: 0 akun `role='tendik'`), pola "self-service edit profil sendiri" tidak relevan untuk kebutuhan ini -- yang dibutuhkan adalah **CRUD administratif (HR/admin kelola data siapa pun)**, sebenarnya kemampuan yang bahkan Dosen sendiri TIDAK punya di SIKD.
- User diberi 3 pertanyaan kunci lewat `AskUserQuestion` dan memilih: **(1)** SIKD boleh CREATE record tendik baru (bukan cuma UPDATE), **(2)** cakupan field **selengkap tabel SIMDA aslinya** (termasuk NIK/rekening bank/NPWP/foto -- override keputusan privasi-minimalis yang dipakai versi awal `DataTendik`/view), **(3)** pembuatan akun LOGIN tetap **langkah terpisah manual** lewat Kelola User (konsisten pola Dosen, TIDAK auto-provisioning).

Implementasi:
- OK `simda_dosen.DataTendik` **diekspansi penuh** -- `db_table` diganti dari `master"."v_tendik_ringkas` (view) ke **`master"."data_tendik`** (tabel mentah), field ditambah lengkap sesuai skema asli SIMDA (`nik`, `jenis_kelamin`, `tempat_lahir`, `tgl_lahir`, `pendidikan_terakhir`, `no_hp`, `email`, `tgl_mulai_kerja`, `nama_bank`, `no_rekening`, `atas_nama_rekening`, `foto`, `agama_id`, `status_pernikahan`, `alamat_domisili`, `provinsi_domisili_id`, `kabupaten_domisili_id`, `kode_pos`, `bidang_keahlian`, `no_sk_pengangkatan`, `tgl_sk_pengangkatan`, `npwp`, `sso_user_id`) + property `unit_kerja_nama` baru.
- OK **`simda_dosen.UnitKerja`** (model baru) -- mirror `master.unit_kerja` (katalog organisasi, BUKAN data sensitif, jadi langsung ke tabel mentah tanpa view, sama prinsip dengan `master.mata_kuliah`), dipakai dropdown Unit Kerja di form.
- OK `provinsi_domisili_id`/`kabupaten_domisili_id` **SENGAJA tidak diberi dropdown** di form (field disimpan tapi tidak diedit lewat UI) -- meniru gap yang SUDAH ADA di form Profil Dosen (app `wilayah` SIMDA belum di-mirror di SIKD sama sekali di kedua tempat), bukan celah baru yang diperkenalkan fitur ini.
- OK `simda_dosen/forms.py::DataTendikForm` (ModelForm baru, BEDA dari Profil Dosen yang HTML mentah tanpa Form class) -- dropdown `unit_kerja_id`/`jenis_kepegawaian_id`/`status_kepegawaian_id`/`golongan_id`/`agama_id` (semua IntegerField mentah, bukan FK Django asli karena lintas-database) diisi manual di `__init__` dari model Publik/mirror terkait, dibungkus `try/except DatabaseError` (pola sama `get_pejabat_aktif`) supaya form tetap bisa dibuka kalau salah satu tabel referensi belum ke-grant.
- OK `simda_dosen/views.py::daftar_tendik/tambah_tendik/ubah_tendik/toggle_aktif_tendik` + `simda_dosen/urls.py` (`/simda-dosen/tendik/...`) -- **admin-only** (`_bisa_kelola_data_tendik`, BUKAN discope `dapat_kelola` seperti Kelola User -- data tendik tidak punya konsep fakultas/prodi untuk dipetakan, dan field-nya data HR sensitif terpusat). **Toggle aktif/nonaktif, BUKAN hapus** -- sama pola `KelompokPresensi`, data tendik terhubung ke riwayat presensi/jabatan struktural.
- OK Template `templates/simda_dosen/daftar_tendik.html` (list + cari) & `tendik_form.html` (form dikelompokkan per section: Identitas/Kontak & Alamat/Kepegawaian/Rekening Bank/Foto & Status, meniru struktur sub-section tab "Profil" Dosen tapi satu form, bukan tab). Nav link "Kelola Data Tendik" ditambahkan di `templates/base.html` section Administrasi (gated `role == 'admin'`).
- ⚠️ **PERLU LANGKAH MANUAL DI SIMDA (belum dijalankan user)**: `C:\unisan\simda\tambah_akses_tendik_penuh.sql` -- menaikkan `sikd_rw` dari cuma-baca (view) ke **baca-tulis PENUH** ke `master.data_tendik` (SELECT/INSERT/UPDATE/DELETE + akses sequence `data_tendik_id_seq`) + SELECT ke `master.unit_kerja` (dropdown). Ini **perluasan akses tulis SIKD ke SIMDA yang signifikan** (termasuk field sensitif NIK/rekening/NPWP) -- keputusan sadar sesuai pilihan eksplisit user, bukan default diam-diam. View lama `master.v_tendik_ringkas` boleh dibiarkan ada (tidak dipakai lagi oleh kode SIKD, tidak mengganggu).
- Pembuatan record `DataTendik` dari SIKD **TIDAK** otomatis membuat akun login (`accounts.User`) -- tetap 2 langkah terpisah (isi data tendik di sini, lalu buat akun di Kelola User dengan NIP Yayasan yang sama persis), konsisten pola Dosen.
- Test baru: `simda_dosen/tests.py` (app ini BELUM PUNYA test sebelumnya) -- `DataTendikFormTest` (dropdown terisi dari SIMDA + fallback DatabaseError), `KelolaDataTendikViewTest` (akses admin-only, cari, toggle aktif) -- **belum dites di VPS**, perlu dijalankan setelah deploy DAN setelah SQL grant dijalankan (kalau grant belum jalan, halaman tetap bisa dibuka tapi simpan/create akan gagal permission denied -- test yang di-mock tidak kena ini, tapi pemakaian sungguhan di produksi iya).
- ⚠️ **Bug ditemukan setelah deploy pertama**: dropdown Role di form Kelola User (`templates/accounts/tambah_user.html`/`edit_user.html`) di-hardcode manual dan **tidak pernah menyertakan "Tendik"** (padahal sudah lama ada di `ROLE_CHOICES`), dan field `nip_yayasan` (kunci pencocokan ke `DataTendik`) **belum pernah diekspos di form Kelola User sama sekali** -- akibatnya akun tendik tidak bisa dibuat sama sekali, memblokir seluruh alur "Kelola Data Tendik" di atas. Diperbaiki: opsi "Tendik" + field NIP Yayasan ditambahkan ke kedua template, `accounts/views.py::tambah_user`/`edit_user` dibaca/simpan `nip_yayasan`-nya. Test baru: `accounts/tests.py::TambahUserNipYayasanTest` (app ini juga baru punya test pertama kali).

**[DASHBOARD TENDIK — selesai per 2026-08-01]** Setelah akun tendik bisa dibuat & presensi jalan, user minta dianalisis: dashboard utama portal (halaman `/` setelah login) modelnya 100% berbasis Tri Dharma dosen, tidak cocok untuk tendik. **Analisis** (lihat riwayat percakapan) menemukan: `dashboard/views.py::index()` sebenarnya BINER, bukan "sama seperti dosen" -- role `admin/rektorat/biro/dekan/wadek/operator/kaprodi/sekprodi` semua dapat dashboard **oversight** (rekap DOSEN LAIN: total dosen, chart distribusi per fakultas/pendidikan/jabfung), cuma role `dosen` sendiri dapat dashboard **personal** (kelengkapan profil, BKD, link Tri Dharma). Role `tendik` tidak match cabang mana pun, jatuh ke `context={}` lalu ke `{% else %}` template yang secara harfiah berkomentar "Dashboard Admin/Rektorat/Dekan" -- salah kategori sama sekali (bukan cuma "mirip dosen"), karena tendik tidak mengawasi dosen mana pun.
- ✅ **Cabang dashboard baru khusus `role == 'tendik'`** (`dashboard/views.py`, sebelum baris `return render(...)`) -- SENGAJA bukan Tri Dharma dan bukan oversight-dosen, isinya reuse penuh yang sudah ada: identitas dari `get_simda_tendik_or_none(user)`, ringkasan presensi bulan berjalan dari `presensi.rekap.rekap_bulanan_user()` (hari hadir/total jam kerja/target/selisih), status presensi hari ini (`Presensi` langsung), dan status izin/cuti (`IzinCuti`, termasuk hitung yang masih menunggu approval). Tidak ada logika baru, murni pakai fungsi yang sudah dibangun untuk presensi.
- ✅ **`templates/dashboard/index.html`** -- cabang `{% elif request.user.role == 'tendik' %}` baru disisipkan SEBELUM `{% else %}` (oversight admin/dekan), supaya tendik tidak lagi jatuh ke situ. Style CSS didefinisikan ulang lokal di cabang ini (`.welcome-card-tendik`/`.stat-card-tendik`) -- konsisten dengan pola yang sudah ada (cabang dosen & cabang admin/dekan juga masing-masing punya `<style>` sendiri, tidak share, karena cuma satu cabang yang render per request).
- ✅ **2 perbaikan kecil terkait di `templates/base.html`**: (1) label role sidebar sekarang eksplisit menyebut "Tendik" (dulu jatuh ke `{% else %}Dosen{% endif %}`, jadi tendik terlihat berlabel "Dosen"); (2) section nav "Manajemen" (Rekap Data, Export Laporan) diubah dari gate negatif `{% if user.role != 'dosen' %}` jadi `{% if user.role != 'dosen' and user.role != 'tendik' %}` -- sebelumnya bocor ke tendik walau kedua halaman itu 100% berbasis data dosen (`dashboard:rekap` & `laporan:index` sama-sama pakai queryset dosen, kosong/tidak relevan untuk tendik).
- Tidak ada model/migrasi baru -- murni membaca data yang sudah ada (`Presensi`, `IzinCuti`, `simda_dosen.DataTendik` via helper yang sudah dibuat sebelumnya).
- Test baru: `dashboard/tests.py::DashboardTendikTest` (app ini juga baru punya test pertama kali) -- akses dashboard, tidak ada context Tri Dharma, hitungan izin menunggu, dan regresi nav "Rekap Data" tidak lagi bocor ke tendik.

**[PENGELOMPOKAN SIDEBAR ADMIN — selesai per 2026-08-02]** User minta dianalisis: sidebar admin terlalu ramai (±28 item top-level, 13 di antaranya rata di satu section "Administrasi"). Dianalisis 2 opsi (submenu collapsible vs gabung jadi tab dalam 1 halaman) dan dipilih submenu collapsible untuk sidebar (resiko lebih rendah, tidak ubah URL/view).
- OK Section "Administrasi" dipecah jadi 3 submenu collapsible (Bootstrap `collapse`, JS-nya sudah ikut ter-load lewat Tabler core, dipakai juga oleh navbar-toggler mobile yang sudah ada) -- **Kepegawaian** (Kelola User, Kelola Data Tendik), **Presensi (Admin)** (Dashboard/Tinjau/Persetujuan Izin/3 Laporan/Pengaturan Presensi/Laporan Serdos), **Sistem** (Log Aktivitas/Data Master/Django Admin). Default collapsed (tidak auto-expand ke grup yang sedang aktif -- disederhanakan, trade-off kecil demi template yang jauh lebih simpel).
- OK Section "Data Dosen" (Profil Dosen/Kualifikasi/Kompetensi/Pelaks. Pendidikan-Penelitian-Pengabdian/Penunjang/Reward/Laporan BKD/Data Dokumen) **dikeluarkan dari tampilan admin** -- admin bukan dosen, tidak punya profil sendiri di situ, section ini murni bikin ramai tanpa guna untuk admin (tetap tampil untuk dosen & operator).
- Tidak ada perubahan URL/view/akses sama sekali -- murni reorganisasi tampilan `templates/base.html`.

**[LAPORAN BULANAN JAM KERJA DIGABUNG KE LAPORAN PRESENSI INTERNAL — selesai per 2026-08-02]** User minta filter fakultas/prodi + blok pengesahan ditambahkan ke Laporan Bulanan Jam Kerja, lalu menanyakan apakah sebaiknya digabung saja ke Laporan Presensi Internal karena filter & pengesahan di situ sudah berfungsi. **Analisis** (lihat riwayat percakapan): kedua laporan menjawab pertanyaan BEDA (Internal: "jam berapa masuk/pulang tiap hari" -- grid ±31 kolom tanggal; Bulanan: "total jam kerja vs target -- dasar penggajian" -- tabel ringkas 7 kolom) dan Internal sudah mepet di kertas F4 (makanya Masuk/Pulang ditumpuk 1 kolom, bukan 2) -- memaksakan kolom ringkasan nempel di ujung grid beresiko tidak muat/terlalu sesak. Keputusan: **gabung di level HALAMAN (satu form filter + satu blok pengesahan dipakai bersama), TAPI TETAP 2 keluaran terpisah** (tombol unduh beda untuk Grid Harian vs Ringkasan Jam Kerja) -- bukan satu tabel dipaksakan jadi satu.
- OK `/presensi/laporan-bulanan/` (URL lama) sekarang **cuma redirect** ke `/presensi/laporan-internal/` (query string bulan/tahun dipertahankan, nama parameternya sama persis dengan `LaporanInternalForm`) -- `templates/presensi/laporan_bulanan.html` **dihapus** (dead code, view tidak pernah render-nya lagi). Nav sidebar "Laporan Bulanan Jam Kerja" dihapus, digabung ke label "Laporan Presensi Internal & Bulanan Jam Kerja".
- OK `halaman_laporan_internal` sekarang sekaligus menghitung `halaman_ringkasan` (hasil `rekap.laporan_bulanan_jam_kerja()`, dipaginate) memakai queryset yang SAMA (sudah difilter kategori/fakultas/prodi lewat `_dapatkan_user_qs_dari_form_internal` yang sudah ada) -- Ringkasan Jam Kerja otomatis kebagian filter fakultas/prodi yang sebelumnya tidak ada di halaman lama.
- OK **Blok pengesahan** (kota/tanggal cetak/jabatan/nama/id_penandatangan, pola manual sama seperti Detail Presensi Bulanan) sekarang dipakai bersama oleh KEDUA bentuk laporan -- helper `_parameter_penandatangan(request)` (di-generalisasi dari `_parameter_penandatangan_detail`, nama fungsi & docstring diperbarui, 3 pemanggil disesuaikan) dipanggil di halaman & keempat ekspor (Grid Harian PDF/Excel, Ringkasan PDF/Excel).
- OK **Ekspor PDF Ringkasan Jam Kerja BARU** (`presensi/laporan_bulanan_pdf.py`, A4 portrait -- beda dari Internal yang F4 landscape lebar, karena Ringkasan cuma 8 kolom ringkas) -- sebelumnya laporan ini cuma punya Excel.
- OK Logika Excel Ringkasan dipindah dari inline di `views.py` ke `presensi/laporan_bulanan_excel.py` (konsisten pola modul render_* terpisah yang sudah dipakai Internal/Detail/Serdos) + ditambah label filter (reuse `_label_filter` dari `laporan_internal_pdf.py`) & blok pengesahan.
- OK `render_pdf_laporan_internal()`/`render_excel_laporan_internal()` juga ditambah kwarg pengesahan opsional (sebelumnya cuma Detail Presensi Bulanan yang punya blok ini).
- ⚠️ **Bug ditemukan & diperbaiki saat mengerjakan ini** (tidak terkait langsung, ditemukan tidak sengaja): `laporan_detail_pdf.py::render_pdf_detail_presensi` memanggil `timezone.localdate()` tapi modulnya TIDAK PERNAH mengimpor `django.utils.timezone` -- laten (belum pernah ke-trigger karena satu-satunya pemanggil, `views.py`, selalu mengisi `tanggal_cetak` konkret lewat `_parameter_penandatangan`), tapi akan `NameError` kalau fungsi ini dipanggil langsung tanpa `tanggal_cetak`. Sudah ditambah importnya.
- Test lama `LaporanBulananViewTest` (menguji halaman lama yang sudah tidak ada) **diganti total**: `LaporanBulananRedirectTest` (redirect + query string + permission di tujuan akhir) dan `LaporanRingkasanJamKerjaTest` (semua assertion lama dipindah ke URL `/presensi/laporan-internal/`, ditambah test filter kategori/fakultas baru + ekspor PDF baru + ekspor dengan pengesahan). Test baru juga ditambah di `LaporanInternalViewTest` (context `halaman_ringkasan` muncul, ekspor dengan pengesahan tidak error).

**[KOREKSI: ikon drill-down Rekap Data dilengkapi, section "Data Dosen" TIDAK dikembalikan ke admin — selesai per 2026-08-03]** User melaporkan regresi: setelah section "Data Dosen" dihapus dari sidebar admin (lihat poin "PENGELOMPOKAN SIDEBAR ADMIN" di atas), admin merasa kehilangan cara melihat kinerja dosen lain (profil/pengajaran/penelitian/dst). **Analisis ulang** (lihat riwayat percakapan) menemukan bahwa asumsi awal SALAH: link-link di section "Data Dosen" (`profil:index`, `pendidikan:index`, dst di sidebar) **TIDAK PERNAH membawa parameter `?dosen_id=`** — mengkliknya selalu menampilkan data milik akun yang login sendiri, bukan dosen manapun. Jadi section itu **tidak pernah benar-benar berfungsi untuk melihat dosen LAIN**, baik sebelum maupun sesudah dihapus dari admin — mengembalikannya tidak akan menyelesaikan apa pun (admin akan tetap lihat profil/kinerja kosong miliknya sendiri).
- Mekanisme yang SUNGGUH berfungsi untuk lintas-dosen adalah **Rekap Data** (`dashboard:rekap`, menu "Manajemen" — TIDAK disentuh oleh perubahan sidebar sebelumnya, tetap ada untuk admin/dekan/wadek/operator/kaprodi/sekprodi/rektorat/biro dengan cakupan yang sudah benar per role). Sebelum perbaikan ini, baris aksi per-dosen di halaman itu (`templates/dashboard/rekap.html`) cuma punya 5 ikon (Profil, "Kinerja" [sebenarnya Pendidikan], PDF, Laporan BKD, Data Dokumen) — **Penelitian, Pengabdian, Penunjang, Reward, Kualifikasi, dan Kompetensi TIDAK PUNYA ikon sama sekali** (cuma bisa diakses lewat mengetik manual `?dosen_id=` di address bar) — celah ini sudah ada SEBELUM sesi ini, bukan akibat perubahan sidebar.
- ✅ **Perbaikan**: 6 ikon baru ditambahkan ke baris aksi `templates/dashboard/rekap.html` — Penelitian (`penelitian:index`), Pengabdian (`pengabdian:index`), Penunjang (`penunjang:index`), Reward (`reward:index`), Kualifikasi (`profil:kualifikasi_index`), Kompetensi (`profil:kompetensi_index`) — semuanya `?dosen_id={{ r.dosen.id }}`, pola identik dengan 5 ikon yang sudah ada. Ikon "Kinerja" yang salah label juga diperjelas jadi "Pendidikan". Ini otomatis berlaku untuk SEMUA 8 role yang punya akses Rekap Data (bukan cuma admin), karena view-view target (`penelitian:index` dst) sudah lama menerima `dosen_id` lewat pola role-list check yang sama (`user.role in ['admin','kaprodi','sekprodi','operator','dekan','wadek','rektorat','biro']`) — tidak ada perubahan di view/permission, murni tambah link di template.
- Section "Data Dosen" **sengaja TIDAK dikembalikan** ke admin — analisis git log mengonfirmasi section itu historisnya cuma untuk role `dosen`/`operator`/`admin`, dan untuk admin selalu jadi jalan buntu (self-view kosong) sejak awal, jadi penghapusannya dari sidebar admin (sesi sebelumnya) sudah tepat; yang salah cuma diagnosis akar masalahnya, bukan tindakannya.
- Tidak ada perubahan model/migrasi/view/test — murni tambah 6 `<a>` link di satu template, diverifikasi lewat `manage.py check` + reverse semua nama URL baru + parse template (Postgres lokal tidak bisa diakses untuk jalankan test suite penuh, lihat Bagian 8) — **sudah dites di VPS, ikon tampil semua**.
- ⚠️ **Bug kecil ditemukan & diperbaiki setelah deploy pertama**: ikon Pengabdian pakai `ti-hand-heart` -- nama ini TIDAK ADA di Tabler Icons webfont (dicek langsung ke CSS yang di-load `base.html`), jadi blank/tidak render. Diganti `ti-heart-handshake` (sudah diverifikasi ada di CSS). Ikon lain (`ti-flask`, `ti-puzzle`, `ti-award`, `ti-school`, `ti-certificate`) sudah benar sejak awal.

**[SUMBER DATA DOSEN DI EKSPOR REKAP DATA — selesai per 2026-08-03]** User menanyakan apakah ekspor Excel/PDF di Rekap Data (Excel Rekap Detail, PDF Rekap, PDF per-dosen, Statistik Profil) sudah pakai data dosen dari SIMDA, bukan dari tabel akun (`accounts.User`). **Analisis** (lihat riwayat percakapan) menemukan campuran tidak konsisten di `laporan/views.py`:
- **Statistik Kinerja** — aman, murni hitungan agregat, tidak ada identitas dosen sama sekali.
- **Excel Rekap Detail, PDF Rekap, PDF per-dosen (`export_excel_rekap`/`export_pdf_rekap`/`export_pdf_dosen`)** — SEBELUM perbaikan: Nama/NIDN/Fakultas/Prodi diambil dari `accounts.User` (nama TANPA gelar, `get_full_name()` biasa), sedangkan Jabfung/Pendidikan/Status/Kelengkapan Profil sudah benar dari SIMDA (`DataDosen`, lewat `get_simda_dosen_or_none`) -- **identitas dosen split 2 sumber dalam satu baris yang sama**. Yang lebih mencolok: `dashboard/views.py::rekap()` (halaman yang jadi pintu masuk ke ekspor-ekspor ini) SUDAH lebih dulu diperbaiki pakai `profil.nama_lengkap_gelar` (nama+gelar dari SIMDA, ada komentar eksplisit di kodenya kenapa) -- tapi perbaikan itu tidak ikut diterapkan ke 3 ekspor ini, padahal `profil` sudah di-fetch di baris yang sama untuk field lain. Akibatnya nama yang tampil di layar Rekap Data (dengan gelar) beda dengan yang tercetak di Excel/PDF (tanpa gelar).
- **Statistik Profil (`export_excel_statistik_profil`)** — hitungan pendidikan/jabfung/status SUDAH benar dari SIMDA, tapi kunci pengelompokan "dosen ini masuk fakultas/prodi yang mana" (untuk menentukan populasi tiap baris) diambil dari `User.kode_fakultas`/`kode_prodi`, bukan `DataDosen.kode_fakultas`/`kode_prodi` milik SIMDA sendiri -- kalau kedua sumber sempat tidak sinkron, hitungan bisa masuk ke baris fakultas/prodi yang salah.
- ✅ **Perbaikan** (dikonfirmasi user lewat `AskUserQuestion`, 2 keputusan: perbaiki nama+gelar, DAN perbaiki fakultas/prodi supaya dari SIMDA): helper baru `laporan/views.py::_identitas_tampilan_dosen(dosen, profil)` -- utamakan `profil.nama_lengkap_gelar`/`profil.kode_fakultas`/`profil.kode_prodi` kalau `profil` (DataDosen) tersedia, fallback ke `dosen.get_full_name()`/`dosen.kode_fakultas`/`dosen.kode_prodi` (User) kalau dosen belum ter-mirror di SIMDA -- dipakai di ketiga ekspor identitas (`export_excel_rekap`, `export_pdf_rekap`, `export_pdf_dosen`). `export_pdf_dosen` juga sekalian diperbaiki: field "Bidang Keahlian" yang sebelumnya HARDCODE `'-'` sekarang pakai `profil.bidang_keahlian_nama` (data SIMDA sudah tersedia, cuma belum pernah dipakai).
- ✅ **`export_excel_statistik_profil::get_profil_counts`** — populasi per Fakultas/Prodi sekarang dibucket dari `DataDosen.objects.using('simda').filter(kode_fakultas=...)`/`filter(kode_prodi=...)` langsung (bukan lewat `accounts.User` dulu). Hitungan Sertifikasi Serdos (satu-satunya sub-hitungan yang harus tetap lewat `accounts.User`, karena `Sertifikasi` FK ke `User` bukan `DataDosen`, beda database) dicocokkan lewat `user__nidn__in=nidn_list` (NIDN dari SIMDA), bukan lagi `user__in=<queryset User>`.
- NIDN itu sendiri TIDAK diubah sumbernya (tetap dari `dosen.nidn`/`accounts.User`) -- NIDN memang jadi kunci pencocokan ke SIMDA di awal, jadi nilainya identik di kedua sumber untuk dosen yang sudah match; tidak ada gunanya diganti.
- Test baru: `laporan/tests.py` (app ini sebelumnya nol test) -- `IdentitasTampilanDosenTest` (unit test helper murni, 3 skenario: profil ada, profil None, profil ada tapi kode_fakultas/prodi kosong), `ExportRekapNamaSimdaTest` (view test, verifikasi Excel/PDF Rekap pakai nama+gelar+fakultas dari SIMDA yang di-mock, termasuk PDF pakai `bidang_keahlian_nama`, dan fallback ke nama akun kalau profil None), `ExportExcelStatistikProfilBucketingTest` (verifikasi `DataDosen` di-query dengan `kode_fakultas`/`kode_prodi` langsung, bukan lewat `accounts.User`) -- **belum dites di VPS**, perlu dijalankan setelah deploy (Postgres lokal tidak bisa diakses untuk jalankan test suite penuh, lihat Bagian 8; diverifikasi lokal lewat `manage.py check` + compile + import-check saja).

**[NAMA APLIKASI DIPERJELAS MENCAKUP TENDIK — selesai per 2026-08-03]** User menanyakan apakah nama aplikasi yang tampil ke pengguna masih "Sistem Informasi Kinerja Dosen" saja, padahal portal sudah dipakai untuk Dosen DAN Tendik. **Analisis**: teks kepanjangan itu literal muncul di 4 tempat -- Login (`templates/accounts/login.html`), footer semua halaman (`templates/base.html`), dan 2 kartu selamat datang di `templates/dashboard/index.html` (baris ~74 untuk branch dosen-personal, baris ~456 untuk branch admin/dekan/wadek/kaprodi/sekprodi/rektorat/biro/operator yang SEKARANG semuanya juga mengelola/lihat data Tendik).
- ✅ **Diubah jadi "...Dosen dan Tendik"** di 3 tempat: Login, footer, dan kartu selamat datang branch admin/oversight (bukan dosen-personal). Branch dosen-personal (baris ~74) **sengaja TIDAK diubah** -- itu memang halaman Tri Dharma personal milik dosen sendiri, benar kalau tetap bicara "Kinerja Dosen". Branch tendik (dashboard) sudah lebih dulu benar sejak dibuat -- tidak pernah menyebut "Kinerja Dosen" sama sekali.
- Akronim **"SIKD"** (navbar brand, `<title>`, footer "SIKD v1.0") **sengaja TIDAK diganti** -- dipakai luas juga sebagai istilah umum "portal ini" di kode internal (pesan error SSO, komentar, label admin/ekspor), bukan cuma kepanjangan literal. Rebranding akronim jadi pekerjaan besar untuk manfaat kosmetik kecil, institusi lazim tetap pakai singkatan lama meski cakupan berkembang.
- Tidak ada perubahan model/view/test -- murni 3 baris teks di 3 template, diverifikasi lewat parse template + `manage.py check`.

**[AUDIT RBAC SELURUH PORTAL & PERBAIKAN CELAH KRITIS — selesai per 2026-08-03]** User minta dianalisis apakah semua proses sudah benar di semua level (role). 4 agent riset paralel (Tri Dharma, Presensi, Laporan/Dashboard, Kelola Tendik & Kelola User) menemukan beberapa celah nyata, bukan cuma kosmetik. 4 temuan KRITIS sudah diperbaiki (dikonfirmasi user), sisanya (Sedang/Ringan) dicatat sebagai temuan belum dikerjakan.

✅ **Kritis #1 — 20 tombol "Tambah" di 5 modul Tri Dharma (`pendidikan`/`penelitian`/`pengabdian`/`penunjang`/`reward`) tidak mengecek `dapat_kelola()` sebelum menyimpan.** Semua `tambah_*` di 5 app itu memanggil `_target_user(request, from_post=True)` (resolver READ-only, 8-role list TANPA gerbang `dapat_kelola` -- ini benar untuk keperluan baca lintas-dosen) lalu LANGSUNG `.create()` tanpa pengecekan lanjutan -- padahal `edit_*`/`hapus_*` di app yang SAMA sudah benar mengecek `dapat_kelola(obj.user)`. Akibatnya: Rektorat/Biro (seharusnya baca-saja) bisa MEMBUAT data untuk dosen manapun; Dekan/Kaprodi/Operator bisa membuat data untuk dosen DI LUAR fakultas/prodi mereka lewat `dosen_id` sembarangan. Diperbaiki: tambah baris `if not request.user.dapat_kelola(target_user): ... return redirect(...)` persis setelah `target_user = _target_user(request, from_post=True)` di SEMUA 20 pemanggilan (pakai `replace_all` per file karena barisnya identik persis di tiap fungsi). Test baru (app-app ini SEBELUMNYA NOL test): `pendidikan/tests.py`, `penelitian/tests.py`, `pengabdian/tests.py`, `reward/tests.py`, `penunjang/tests.py` (masing-masing `Tambah*AksesTest`, 1 view representatif per app karena pola bug & fix-nya identik) -- verifikasi dekan beda fakultas ditolak, rektorat ditolak, dekan fakultas sama diterima.

✅ **Kritis #2 — `profil.tambah_dokumen_lain` (satu endpoint, bug sejenis #1 + 1 bug tambahan).** `_dokumen_target_user()` membaca `dosen_id` dari `request.GET`, tapi dipanggil dari alur POST lewat `request.POST.get('dosen_id')` sebagai syarat -- mismatch GET/POST yang bisa dieksploitasi (isi `dosen_id` di query string DAN body sekaligus), dan TIDAK ada `dapat_kelola` sama sekali. Diperbaiki: baca `dosen_id` langsung dari `request.POST`, lalu cek `dapat_kelola` sebelum simpan (pola sama seperti #1). `edit_dokumen_lain`/`hapus_dokumen_lain` sudah benar sejak awal, tidak disentuh. Test baru: `profil/tests.py` (app ini juga sebelumnya nol test) -- termasuk `test_mismatch_get_post_dosen_id_tetap_ditolak` khusus untuk celah GET/POST itu.

✅ **Kritis #3 — `export_pdf_dosen` (`laporan/views.py`) cuma menolak role `dosen` yang melihat dosen lain, tidak mengecek cakupan fakultas/prodi untuk role lain, dan tidak mengecek `tendik` sama sekali.** Dekan/Kaprodi bisa mengambil PDF kinerja dosen dari fakultas/prodi lain; akun Tendik bisa mengambil PDF dosen manapun. Ditemukan juga bug laten: `messages`/`redirect` dipakai tapi TIDAK PERNAH diimpor di file ini -- baris pengecekan pertama selalu `NameError` sebelum sempat mencapai baris kedua yang sebenarnya benar (gagal "aman" jadi 500, bukan bocor, tapi tetap dead code). Diperbaiki: satu pengecekan bersih `if request.user.id != target_dosen.id and not get_dosen_queryset(request.user).filter(id=target_dosen.id).exists(): return HttpResponseForbidden(...)` -- diri sendiri selalu boleh, selain itu harus masuk cakupan `get_dosen_queryset` (sama seperti Rekap Data: admin/rektorat/biro semua, dekan/wadek/operator fakultas sendiri, kaprodi/sekprodi prodi sendiri, `tendik` otomatis `none()` jadi selalu ditolak). `get_object_or_404` yang tadinya diimpor inline di dalam fungsi dipindah ke import level modul (satu-satunya pemakaian di file ini).

✅ **Kritis #4 — `export_excel_statistik_kinerja` & `export_excel_statistik_profil` (`laporan/views.py`) SAMA SEKALI tanpa pengecekan role.** Siapa pun yang login -- termasuk akun `dosen`/`tendik` yang di app ini semestinya selalu dapat `User.objects.none()` -- bisa langsung unduh statistik agregat SELURUH institusi (semua fakultas/prodi) lewat `/laporan/excel/statistik-kinerja/` atau `/statistik-profil/`. Link ke keduanya di `templates/dashboard/rekap.html` juga tidak dibungkus gate role apapun. Diperbaiki: tambah `if request.user.role not in ['admin', 'rektorat', 'biro']: return HttpResponseForbidden(...)` di awal kedua fungsi -- dibatasi ke role yang MEMANG berhak lihat SEMUA fakultas/prodi tanpa batas cakupan (konsisten dengan `get_dosen_queryset`'s "lihat semua" tier), BUKAN `dapat_kelola`-scoped per-fakultas/prodi seperti Rekap Detail, karena laporan ini secara desain memang institusi-wide (loop semua Fakultas/Prodi tanpa filter). Test baru: `laporan/tests.py::StatistikExportAksesTest` + `ExportPdfDosenAksesTest`.

**Temuan LAIN (Sedang/Ringan) yang BELUM diperbaiki, dicatat untuk keputusan lanjutan:**
- Alur persetujuan presensi (`tinjau_presensi`/`putuskan_presensi`/`putuskan_lembur`/`tinjau_izin`/`putuskan_izin`, `presensi/views.py`): karena `dapat_kelola()` mengizinkan diri sendiri (`self.id == target_user.id`) dan tidak membedakan target dosen vs sesama pejabat, seorang atasan bisa menyetujui presensi/izin/lembur MILIKNYA SENDIRI, dan bisa menyetujui milik sesama pejabat (dekan lain/wadek) di fakultas yang sama.
- `_bisa_tinjau_presensi` (`presensi/views.py`) salah pakai `ROLE_PENGELOLA_SCOPED` (dipinjam dari Kelola User, untuk keperluan TULIS) buat menggerbangi tier BACA-SAJA presensi (Dashboard/Data/3 Laporan) -- akibatnya Rektorat/Biro (seharusnya bisa baca-semua tanpa edit, sama seperti di `laporan`/`dashboard` app) malah ke-block total (403) dari fitur-fitur itu. Bug fungsional, bukan risiko keamanan (under-, bukan over-permissioning).
- Semua view Hapus di 7 modul Tri Dharma tidak mensyaratkan `request.method == 'POST'` (beda dengan Tambah/Edit yang sudah benar) -- bisa terpicu lewat GET, celah CSRF minor.
- `/presensi/paraf/` & `POST /api/presensi/paraf` tidak ada gerbang role server-side (cuma disembunyikan dari nav lewat UI) -- kemungkinan besar disengaja (`ParafDosen` sendiri generik per-user, tidak ada kebocoran lintas-user), tapi beda dari framing "dosen-only" di dokumentasi awal.
- `dashboard/views.py::index()` (branch oversight admin/dekan/dst) menghitung "Total Dosen" dengan filter `status_akun='aktif'`, sedangkan `get_dosen_queryset`/`rekap()` tidak -- angka bisa beda antara Dashboard dan Rekap Data untuk dekan yang sama (isu konsistensi data, bukan keamanan).
- Yang SUDAH diverifikasi benar tanpa celah: Kelola User (role/fakultas/prodi tidak pernah dipercaya dari POST untuk non-admin), Kelola Data Tendik (admin-only konsisten di 4 view), `dapat_kelola()` (tidak ada bug falsy-empty-string), SSO JIT-provisioning (role selalu hardcode `'dosen'`, tidak pernah dari payload SSO), tidak ada endpoint sensitif tanpa `@login_required`.

**[DETAIL PRESENSI BULANAN DIBUKA UNTUK DOSEN & TENDIK (self-service) — selesai per 2026-08-03]** User minta akses Detail Presensi Bulanan (`/presensi/laporan-detail/`) dibuka untuk level dosen/tendik juga (sebelumnya cuma role `dapat_kelola`-scoped lewat `_bisa_tinjau_presensi`), plus filter periode yang ditampilkan.
- ✅ **Akses**: helper baru `presensi/views.py::_bisa_akses_laporan_detail(user)` = `user.role in ("dosen", "tendik") or _bisa_tinjau_presensi(user)`, dipakai di `halaman_laporan_detail`/`export_pdf_laporan_detail`/`export_excel_laporan_detail` (gantikan `_bisa_tinjau_presensi` langsung).
- ✅ **Cakupan self-service, BUKAN buka pencarian lintas-pegawai**: `_cari_target_user_laporan_detail()` sekarang short-circuit di awal -- kalau `request.user.role in ("dosen", "tendik")`, `target_user` SELALU diri sendiri, mengabaikan parameter `q`/`user_id` sama sekali (tidak bisa mengintip pegawai lain lewat manipulasi parameter). Role `dapat_kelola`-scoped (admin dkk) tetap lewat alur cari/pilih pegawai seperti sebelumnya, tidak berubah.
- ✅ **Filter periode**: form bulan/tahun (`_bulan_tahun_dari_request`, sudah ada sebelumnya untuk alur atasan) otomatis langsung tampil untuk dosen/tendik juga, karena `target_user` sudah terisi (diri sendiri) sejak page-load pertama -- tidak perlu cari dulu seperti alur atasan.
- ✅ **Template** (`templates/presensi/laporan_detail.html`) -- context baru `mode_diri_sendiri` (True untuk dosen/tendik) menyembunyikan kartu pencarian nama/NIDN yang tidak relevan untuk self-service, diganti kartu deskripsi singkat. Kartu detail (tabel harian + rekap + form filter bulan/tahun + pengesahan + tombol unduh) tidak diubah, otomatis muncul karena `target_user` sudah ada.
- ✅ **Nav** (`templates/base.html`) -- link "Detail Presensi Bulanan" ditambahkan ke section self-service generik (setelah "Riwayat Presensi"), gated `user.role == 'dosen' or user.role == 'tendik'` -- terpisah dari link yang sama di grup "Presensi (Admin)" untuk role `dapat_kelola`-scoped (dua link berbeda konteks, URL & view sama).
- Test diperbarui: `presensi/tests.py::LaporanDetailViewTest::test_dosen_biasa_tidak_bisa_akses` (asersi 403 lama) **diganti total** jadi beberapa test baru -- dosen/tendik dapat 200 & lihat diri sendiri, dosen tidak bisa mengintip dosen lain lewat `user_id`/`q`, dosen bisa unduh PDF diri sendiri, admin (bukan dosen/tendik) tetap lewat alur cari seperti sebelumnya (`mode_diri_sendiri=False`). Tidak ada model/migrasi baru -- murni perubahan akses + tampilan, `detail_presensi_bulanan()`/render PDF-Excel sudah generik per-`user` sejak awal (tidak ada asumsi dosen-only seperti NIDN wajib), jadi jalan tanpa perubahan untuk tendik.

**[KELOLA USER DIPERSEMPIT KE ADMIN + OPERATOR — selesai per 2026-08-03]** User minta akses menu Kelola User (`accounts/views.py::kelola_user`/`tambah_user`/`edit_user`/`hapus_user`) dipersempit -- sebelumnya (per audit RBAC sebelumnya) 6 dari 8 role privileged bisa akses (admin + `ROLE_PENGELOLA_SCOPED` = dekan/wadek/kaprodi/sekprodi/operator), sekarang cukup admin + operator (fakultas sendiri).
- ✅ **`accounts/views.py::ROLE_PENGELOLA_SCOPED`** diubah dari `['dekan', 'wadek', 'kaprodi', 'sekprodi', 'operator']` jadi `['operator']` -- konstanta ini SPESIFIK untuk gerbang Kelola User di app `accounts`, TIDAK sama dengan `presensi/views.py::ROLE_PENGELOLA_SCOPED` (konstanta lokal terpisah, dipakai untuk Tinjau Presensi dkk, TIDAK disentuh -- dekan/wadek/kaprodi/sekprodi tetap punya akses situ seperti semula). `dapat_kelola()` (`accounts/models.py`, dipakai Tri Dharma/presensi/laporan) juga TIDAK berubah -- masih mencakup dekan/wadek/kaprodi/sekprodi seperti biasa, cuma MENU Kelola User yang dipersempit.
- ✅ Cabang scoping kaprodi/sekprodi (prodi-based) di `kelola_user`/`tambah_user`/`edit_user` dihapus (jadi dead code karena role itu sudah tidak lolos gerbang), cabang dekan/wadek/operator disederhanakan jadi operator-only.
- ✅ **Nav** (`templates/base.html`) -- seluruh grup collapsible "Kepegawaian" (berisi Kelola User + Kelola Data Tendik) sekarang digate `user.role == 'admin' or user.role == 'operator'` (sebelumnya ikut gate luar "Administrasi" yang mencakup dekan/wadek/kaprodi/sekprodi juga) -- supaya dekan/kaprodi/dst tidak melihat grup kosong (Kelola Data Tendik sendiri sudah admin-only sejak awal).
- Test baru: `accounts/tests.py::KelolaUserAksesTest` -- dekan & kaprodi ditolak (302) di kelola_user/tambah_user/edit_user, operator fakultas sendiri & admin tetap diterima (200).
- Tidak ada perubahan model/migrasi -- murni penyempitan gerbang akses + pembersihan dead code, diverifikasi lewat `manage.py check` + compile + import-check (Postgres lokal tidak bisa diakses untuk jalankan test suite penuh, lihat Bagian 8) -- **belum dites di VPS**.

**[TOMBOL TAMBAH DIBATASI KE ADMIN + OPERATOR DI SEMUA MODUL TRI DHARMA — selesai per 2026-08-03]** Lanjutan dari audit RBAC: user minta tombol "Tambah" di Profil Dosen dan semua tab kinerja (Pendidikan/Penelitian/Pengabdian/Penunjang/Reward/Kualifikasi/Kompetensi/Laporan BKD) dinonaktifkan untuk Rektorat/Biro/Dekan/Kaprodi (dikonfirmasi lewat `AskUserQuestion`: **Wadek & Sekprodi ikut dibatasi juga** -- selalu diperlakukan sama dengan Dekan/Kaprodi di seluruh kode; **data diri sendiri TETAP boleh ditambahkan** -- yang dibatasi cuma menambahkan data ATAS NAMA dosen lain). Beda dari perbaikan RBAC sebelumnya (yang cuma menegakkan `dapat_kelola()` yang SUDAH ADA): ini keputusan kebijakan BARU yang mempersempit `dapat_kelola()` lebih lanjut khusus untuk aksi Tambah -- Edit/Hapus TIDAK berubah (dekan/kaprodi tetap bisa edit/hapus data dosen fakultas/prodinya sendiri seperti biasa).
- ✅ **Helper baru** `simda_dosen/utils.py::bisa_tambah_tridarma(user, target_user)` -- `True` kalau `user.id == target_user.id` (self-service, selalu boleh) ATAU (`user.role in ('admin', 'operator')` DAN `dapat_kelola(target_user)`). Dekan/wadek/kaprodi/sekprodi/rektorat/biro yang menargetkan dosen LAIN sekarang selalu `False`, meski `dapat_kelola()` sendiri (dipakai Edit/Hapus, TIDAK diubah) masih `True` untuk mereka dalam cakupan fakultas/prodi.
- ✅ **`simda_dosen/utils.py::resolve_target_user_tambah(request, dosen_id, redirect_url_name)`** -- versi "Tambah" dari `resolve_target_user()` yang sudah ada (dipakai `simpan_profil`, TIDAK diubah -- itu UPDATE profil dasar yang sudah ada, bukan "Tambah" data baru), pakai `bisa_tambah_tridarma` bukan `dapat_kelola` polos.
- ✅ **Endpoint yang diperbaiki** (28 total): 20 `tambah_*` di `pendidikan`/`penelitian`/`pengabdian`/`penunjang`/`reward` (inline check `dapat_kelola` → `bisa_tambah_tridarma`), 6 di `profil` (`tambah_jabfung`/`tambah_pangkat`/`tambah_pendidikan`/`tambah_diklat`/`tambah_sertifikasi`/`tambah_tes`, lewat `resolve_target_user` → `resolve_target_user_tambah`), `profil.tambah_dokumen_lain` (inline check), `kinerja.tambah_bkd` (lewat `resolve_target_user_tambah`).
- ✅ **Konteks `bisa_tambah` baru** ditambahkan ke 7 view `index()`/`kualifikasi_index()`/`kompetensi_index()`/`dokumen_index()`/`bkd_index()` (terpisah dari `bisa_edit` yang tetap dipakai Edit/Hapus) = `bisa_tambah_tridarma(user, target_user) and input_terbuka`.
- ✅ **Template**: SEMUA blok form "Tambah [Fitur]" (identifikasi lewat wrapper `<div class="card border-0 bg-light mb-4">` yang konsisten di semua app) diganti dari `{% if bisa_edit %}` jadi `{% if bisa_tambah %}` -- 28 blok total di `pendidikan/index.html` (7), `penelitian/index.html` (3), `pengabdian/index.html` (4), `penunjang/index.html` (3), `reward/index.html` (3), `profil/index.html` (3: jabfung/pangkat/pendidikan), `profil/kualifikasi.html` (1), `profil/kompetensi.html` (2), `profil/dokumen.html` (1), `kinerja/bkd.html` (1). `profil/index.html` masih punya beberapa `{% if bisa_edit %}` LAIN (field Profil Dasar/`simpan_profil` seperti Foto/KTP/NPWP/tombol Simpan) yang SENGAJA TIDAK disentuh -- itu UPDATE data yang sudah ada, bukan "Tambah" baris baru. Baris `{% if bisa_edit %}` untuk tombol Edit/Hapus per-baris (row-level) juga TIDAK disentuh -- dibedakan lewat wrapper HTML yang mengikutinya (`<div class="card...">` = Tambah, `<button>`/`<div class="d-flex gap-1">` = row action), diverifikasi manual per file sebelum diganti supaya tidak salah sasaran.
- Test baru: `simda_dosen/tests.py::BisaTambahTridarmaTest` (unit test helper murni). Test yang sudah ada di `pendidikan`/`penelitian`/`pengabdian`/`penunjang`/`reward`/`profil` (dari perbaikan RBAC sebelumnya) **diperbarui** -- assertion `test_dekan_fakultas_sama_diterima` (dekan fakultas sendiri BOLEH tambah untuk dosen lain) **dibalik total** jadi `test_dekan_fakultas_sama_ditolak_untuk_dosen_lain` (sekarang TIDAK boleh), ditambah `test_operator_fakultas_sama_diterima` (operator tetap boleh) dan `test_dekan_tetap_bisa_tambah_untuk_diri_sendiri` (self-service tidak terganggu). Test baru juga ditambah: `profil/tests.py::TambahJabfungAksesTest`, `kinerja/tests.py::TambahBkdAksesTest` (keduanya app yang lewat `resolve_target_user_tambah`, SIMDA di-mock lewat `get_simda_dosen_or_none` -- yang diuji murni gerbang akses, bukan penulisan data ke SIMDA).
- ⚠️ **Belum dikerjakan (dicatat, bukan bug baru)**: sub-halaman "Kelola Anggota/Penulis" (co-author/tim, dipakai Penelitian/Publikasi/Paten/Pengabdian/Penunjang/Bahan Ajar untuk menambahkan anggota tim ke record yang SUDAH ADA) masih pakai `dapat_kelola()` biasa, BELUM ikut dibatasi ke admin/operator-only seperti tombol Tambah utama -- artinya dekan/kaprodi fakultas/prodi sendiri masih bisa menambahkan anggota tim ke record dosen lain lewat jalur ini. Perlu keputusan lanjutan apakah ini juga harus dibatasi, atau memang beda kategori (mengelola tim record yang sudah ada vs membuat record baru).
- Tidak ada perubahan model/migrasi -- murni perubahan gerbang akses (views) + tampilan (template), diverifikasi lewat `manage.py check` + compile + import-check + parse template (Postgres lokal tidak bisa diakses untuk jalankan test suite penuh, lihat Bagian 8) -- **belum dites di VPS**.

**[TEMPLATE URL PROFIL RISET (SINTA/SCOPUS/DST) DIJADIKAN PENGATURAN — selesai per 2026-08-04]** User melaporkan link SINTA di dropdown "Link Riset" (Rekap Data/Kelola User) dan tombol di Profil Dosen tidak bisa dibuka (`.../profile/{id}` sudah tidak berfungsi) -- format yang benar `.../authors/profile/{id}/?view=garuda`. Sekaligus diminta: kalau domain/format URL berubah lagi nanti, admin bisa ubah sendiri tanpa perlu edit kode.
- ✅ **5 field baru di `master.Pengaturan`** (singleton settings yang sudah ada, diedit di `/master/` tab "Pengaturan", migrasi `0002_pengaturan_url_template_garuda_and_more.py`): `url_template_sinta`, `url_template_scopus`, `url_template_google_scholar`, `url_template_orcid`, `url_template_garuda` -- masing-masing `CharField` berisi template URL dengan placeholder **`{id}`** (mis. `https://sinta.kemdiktisaintek.go.id/authors/profile/{id}/?view=garuda`), default-nya sudah diisi nilai yang benar (SINTA sekalian diperbaiki jadi format yang dikonfirmasi user berfungsi).
- ✅ **`master/templatetags/master_extras.py::format_link(url_template, id_value)`** -- filter template baru, ganti placeholder `{id}` dengan nilai ID sesungguhnya, return string kosong kalau ID atau template-nya kosong (link tidak dirender). Dipakai di 3 template yang sebelumnya hardcode: `templates/dashboard/rekap.html` (dropdown "Link Riset" per-dosen di Rekap Data), `templates/accounts/kelola_user.html` (dropdown sama di Kelola User), `templates/profil/index.html` (5 tombol "Buka profil ..." di tab Profil Dasar) -- total 15 link (5 layanan x 3 tempat) diganti dari hardcode `https://...{{ profil.id_sinta }}` jadi `{{ pengaturan.url_template_sinta|format_link:profil.id_sinta }}`.
- ✅ **3 view diperbarui** (`dashboard/views.py::rekap`, `accounts/views.py::kelola_user`, `profil/views.py::index`) -- ditambah `'pengaturan': Pengaturan.objects.first()` ke context (sebelumnya cuma dipakai internal lewat `cek_status_input()`, objeknya sendiri belum pernah dikirim ke template ini).
- ✅ **Form Pengaturan** (`templates/master/index.html`, tab Pengaturan) -- 5 input baru "Template URL SINTA/Scopus/Google Scholar/ORCID/Garuda" + catatan cara pakai placeholder `{id}`. `master/views.py::simpan_pengaturan` **sengaja tidak menimpa** field yang dikirim kosong (submit form dengan field kosong = nilai lama tetap dipakai) -- supaya submit form yang salah/tidak sengaja tidak mendadak merusak link riset institusi-wide.
- Django Admin (`master/admin.py::PengaturanAdmin`) otomatis ikut menampilkan 5 field baru ini (tidak ada `fields`/`fieldsets` eksplisit yang membatasi), jadi bisa juga diedit lewat `/admin/` kalau perlu.
- Test baru: `master/tests.py` (app ini sebelumnya nol test) -- `FormatLinkFilterTest` (unit test filter murni, termasuk kasus placeholder-format SINTA yang baru), `SimpanPengaturanUrlTemplateTest` (admin bisa ubah template, submit kosong tidak menghapus nilai lama) -- **belum dites di VPS**, perlu dijalankan setelah deploy DAN migrasi (Postgres lokal tidak bisa diakses, lihat Bagian 8).

**[PROFIL TENDIK: RIWAYAT PENDIDIKAN/PELATIHAN/PRESTASI + UNIT KERJA MANUAL+DROPDOWN — selesai per 2026-08-04]** User minta "Kelola Data Tendik" diperluas jadi Profil Tendik lengkap seperti modelnya Dosen: biodata (sudah ada) + Riwayat Pendidikan/Pelatihan/Prestasi (semua dengan upload dokumen), plus Unit Kerja bisa diisi manual selain dropdown. Diminta analisa dulu sebelum coding.

**Analisis & keputusan (dikonfirmasi user lewat `AskUserQuestion`):**
- Ada 2 pola "riwayat" yang sudah ada di kode: (1) SIMDA-native (Riwayat Jabfung/Pangkat/Pendidikan Dosen -- tabel SIMDA sendiri, FK ke `DataDosen`, admin/self bisa kelola independen dari status akun login), (2) SIKD-lokal (Diklat/Sertifikasi/Reward Dosen -- tabel SIKD, FK ke `accounts.User`). Karena banyak record `DataTendik` di "Kelola Data Tendik" **belum tentu punya akun login** (pembuatan akun sengaja langkah terpisah), pola SIKD-lokal (FK ke `accounts.User`) akan memblokir admin mengisi riwayat untuk tendik yang belum punya akun.
- ✅ **Dipilih pola SIMDA-native**: 3 tabel SIMDA baru, FK ke `DataTendik`, admin-managed sama seperti biodata Tendik yang sudah ada (bukan self-service dulu -- juga dikonfirmasi user, bisa jadi permintaan lanjutan).

**Implementasi:**
- ⚠️ **PERLU LANGKAH MANUAL DI SIMDA (belum dijalankan user)**: `C:\unisan\simda\tambah_tabel_riwayat_tendik.sql` -- CREATE TABLE `master.riwayat_pendidikan_tendik`/`riwayat_pelatihan_tendik`/`riwayat_prestasi_tendik` (masing-masing FK ke `data_tendik`) + grant SELECT/INSERT/UPDATE/DELETE ke `sikd_rw` untuk ketiganya, PLUS naikkan `master.unit_kerja` dari baca-saja jadi bisa INSERT (untuk fitur unit kerja manual baru). Tanpa ini, form Tambah Riwayat & "Ketik Unit Kerja Baru" akan gagal permission denied di produksi (halaman lain tetap jalan).
- ✅ **3 model baru** `simda_dosen/models.py::RiwayatPendidikanTendik`/`RiwayatPelatihanTendik`/`RiwayatPrestasiTendik` (unmanaged, `managed=False`, FK ke `DataTendik`) -- field disederhanakan dari versi Dosen yang setara (RiwayatPendidikanDosen/Diklat/Penghargaan), tanpa field spesifik akademik (semester/tahun_akademik/kode_prodi) yang tidak relevan untuk tendik. File upload (`file_ijazah`/`file_sertifikat`/`file_bukti`) pakai `simda_media_storage` yang sudah ada (fisik tersimpan di server SIMDA, bukan disk SIKD -- lihat catatan `SIMDA_MEDIA_ROOT` di `config/settings.py`).
- ✅ **`simda_dosen/utils.py::get_or_create_unit_kerja(nama)`** -- mirror `get_or_create_bidang_keahlian` (Profil Dosen): cari `UnitKerja` case-insensitive, kalau belum ada buat baru (kode auto dari nama, `jenis='administrasi'` default). Field baru `unit_kerja_baru` (CharField, TIDAK masuk `Meta.fields` `DataTendikForm`) ditambahkan berdampingan dengan dropdown `unit_kerja_id` yang sudah ada -- kalau diisi, `tambah_tendik`/`ubah_tendik` override `instance.unit_kerja_id` dengan hasil `get_or_create_unit_kerja` SEBELUM `form.save()`. Dropdown TIDAK diubah jadi combo/datalist tunggal (2 kontrol terpisah: pilih ATAU ketik) -- lebih jelas untuk form HR formal, lebih sederhana dari sisi implementasi.
- ✅ **10 view baru** di `simda_dosen/views.py` (semua admin-only, gerbang sama `_bisa_kelola_data_tendik`): `detail_tendik` (halaman Profil Tendik, 3 tab) + `tambah_riwayat_X_tendik`/`edit_riwayat_X_tendik`/`hapus_riwayat_X_tendik` untuk X = pendidikan/pelatihan/prestasi (3x3=9). Edit pakai halaman form terpisah (bukan modal JS) -- konsisten dengan pola `tendik_form.html` yang sudah ada di app ini, bukan pola modal yang dipakai app Tri Dharma.
- ✅ **Template baru**: `detail_tendik.html` (3 tab ala Profil Dosen: card "Tambah [Riwayat]" di atas + tabel daftar di bawah tiap tab), `edit_riwayat_pendidikan_tendik.html`/`edit_riwayat_pelatihan_tendik.html`/`edit_riwayat_prestasi_tendik.html` (halaman form penuh, bukan modal). `daftar_tendik.html` ditambah ikon "Profil & Riwayat" per baris. `tendik_form.html` ditambah field "Atau Ketik Unit Kerja Baru".
- Test baru: `simda_dosen/tests.py` -- `GetOrCreateUnitKerjaTest` (unit test helper murni, mock `UnitKerja`), `ProfilTendikAksesTest` (dosen ditolak 403 di semua endpoint baru, admin bisa buka `detail_tendik`), `TambahTendikUnitKerjaBaruTest` (verifikasi `get_or_create_unit_kerja` dipanggil dengan nilai yang diketik -- pakai `patch.object(DataTendik, 'save')` langsung di kelasnya, BUKAN patch nama modul, karena `Meta.model` pada `DataTendikForm` sudah terikat ke kelas asli saat didefinisikan, jadi patch nama di `forms`/`views` module tidak memengaruhi instance yang benar-benar dibuat `form.save()`) -- **belum dites di VPS**, perlu dijalankan setelah deploy DAN setelah SQL script SIMDA dijalankan.
- Tidak ada model/migrasi baru di sisi Tri Dharma/presensi -- murni fitur baru terisolasi di app `simda_dosen`.

**[SELF-SERVICE TENDIK UNTUK RIWAYAT PENDIDIKAN/PELATIHAN/PRESTASI — selesai per 2026-08-04]** Lanjutan langsung dari Profil Tendik di atas -- setelah admin-only terpasang & terverifikasi jalan, user minta dibuka juga untuk tendik sendiri ("agar mereka bisa isi sendiri"). Dikonfirmasi lewat `AskUserQuestion`: **cakupannya cuma 3 Riwayat**, BUKAN biodata dasar (nama/unit kerja/kepegawaian/dst) -- itu tetap admin-only lewat Kelola Data Tendik, tidak berubah.
- ✅ **`simda_dosen/views.py::_bisa_kelola_riwayat_tendik(user, tendik)`** -- helper baru KHUSUS 3 riwayat (beda dari `_bisa_kelola_data_tendik` yang tetap admin-only untuk biodata): `True` kalau admin (siapa pun) ATAU tendik yang bersangkutan SENDIRI (dicocokkan lewat `get_simda_tendik_or_none(user)`, NIP Yayasan -- pola sama dengan `get_simda_dosen_or_none` untuk dosen). Tendik lain (NIP Yayasan beda) selalu `False`.
- ✅ **View baru `profil_riwayat_saya(request)`** (`/simda-dosen/profil-riwayat-saya/`) -- entry point self-service, TIDAK menerima `tendik_id` dari URL (beda dari `detail_tendik` admin yang menerima `tendik_id`) supaya tidak ada celah tebak-ID; tendik-nya sendiri di-resolve dari akun yang login. Kalau NIP Yayasan belum cocok ke SIMDA, dapat pesan error & redirect ke dashboard (pola sama seperti dosen yang NIDN-nya belum cocok). Reuse template `detail_tendik.html` yang sama (context baru `mode_diri_sendiri`), cuma beda cara resolve `tendik`-nya dan tombol "Ubah Biodata" disembunyikan.
- ✅ **9 view CRUD riwayat diubah**: gerbang `_bisa_kelola_data_tendik` (admin-only) diganti `_bisa_kelola_riwayat_tendik` (admin ATAU pemilik sendiri). Redirect setelah aksi juga disesuaikan (`_redirect_setelah_riwayat_tendik`): admin kembali ke `detail_tendik` (URL admin), tendik self-service kembali ke `profil_riwayat_saya`.
- ⚠️ **Bug ditemukan & diperbaiki saat implementasi (sebelum sempat di-deploy)**: revisi awal langsung memanggil `get_object_or_404(DataTendik.objects.using('simda'), ...)` di awal tiap view SEBELUM mengecek role -- akibatnya role yang jelas tidak berhak (dosen, dst) tetap memicu query ke SIMDA dulu baru ditolak, boros query DAN bikin test lama (`test_dosen_biasa_tidak_bisa_tambah_riwayat_pendidikan` dkk) gagal di test environment (SIMDA tidak di-mock di situ, `DatabaseOperationForbidden` bukan 403 bersih). Diperbaiki dengan helper `_role_bisa_riwayat_tendik(role)` -- pre-check MURNI role (`role in ('admin', 'tendik')`), dipanggil PALING AWAL di semua 9 view SEBELUM `get_object_or_404`, baru fetch tendik/riwayat dan cek `_bisa_kelola_riwayat_tendik` yang butuh objeknya.
- ✅ **Nav** (`templates/base.html`) -- link "Riwayat Saya" ditambahkan di section self-service generik, gated `user.role == 'tendik'` saja (beda dari "Detail Presensi Bulanan" yang untuk dosen+tendik).
- Test baru: `simda_dosen/tests.py::ProfilRiwayatSayaAksesTest` -- dosen ditolak, NIP Yayasan tidak cocok redirect dengan pesan, tendik dengan data cocok bisa akses (`mode_diri_sendiri=True`), tendik TIDAK bisa tambah riwayat untuk tendik lain (manipulasi `tendik_id` di URL, dicek beda ID), tendik bisa tambah riwayat miliknya sendiri (ID sama) -- test terakhir pakai `patch.object(RiwayatPendidikanTendik, 'save')` langsung di kelasnya, sama alasan dengan `TambahTendikUnitKerjaBaruTest` sebelumnya (`Meta.model` terikat ke kelas asli saat form didefinisikan). **Belum dites di VPS.**
- Tidak ada model/migrasi/SQL SIMDA baru -- murni perluasan gerbang akses ke view yang sudah ada.

**[SELF-SERVICE TENDIK: EDIT BIODATA POKOK + FOTO — selesai per 2026-08-04]** Lanjutan langsung lagi -- setelah Riwayat self-service jalan, user minta biodata pokok (termasuk foto) juga bisa diedit tendik sendiri, "seperti di level dosen". Ini MENGGANTIKAN keputusan sebelumnya ("Riwayat saja") -- user eksplisit minta diperluas.
- ✅ **`DataTendik.SELF_SERVICE_FIELDS`** (constant baru di model, mirror `DataDosen.SELF_SERVICE_FIELDS`) -- daftar field yang boleh diedit tendik sendiri: `nik`, `nama_lengkap`, `jenis_kelamin`, `tempat_lahir`, `tgl_lahir`, `no_hp`, `email`, `agama_id`, `status_pernikahan`, `alamat_domisili`, `provinsi_domisili_id`, `kabupaten_domisili_id`, `kode_pos`, `bidang_keahlian`, `foto`, `npwp`. Field kepegawaian/struktural/bank (`nip`/`nip_yayasan`/`unit_kerja_id`/`jabatan`/`jenis_kepegawaian_id`/`status_kepegawaian_id`/`golongan_id`/`tgl_mulai_kerja`/`nama_bank`/`no_rekening`/`atas_nama_rekening`/`no_sk_pengangkatan`/`tgl_sk_pengangkatan`/`is_active`/`pendidikan_terakhir`) SENGAJA TIDAK diikutkan -- tetap admin-only lewat Kelola Data Tendik, sama pola dengan field ADMIN/HR milik `DataDosen`. Bedanya dengan constant `SELF_SERVICE_FIELDS` Dosen: constant ini di sini BENAR-BENAR DIPAKAI (bukan cuma dokumentasi) sebagai `Meta.fields` ModelForm -- lihat poin berikut.
- ✅ **`simda_dosen/forms.py::ProfilSayaTendikForm`** (ModelForm baru) -- `Meta.fields = DataTendik.SELF_SERVICE_FIELDS` persis, BUKAN daftar manual terpisah -- artinya field admin-only **tidak bisa disentuh ModelForm ini apa pun yang dikirim di POST** (beda dari `profil/views.py::simpan_profil` milik Dosen yang manual set field satu-satu tanpa Form class -- di sini proteksinya melekat di definisi Form, lebih kuat lewat 1 sumber kebenaran).
- ✅ **View baru `simpan_profil_saya_tendik(request)`** (`/simda-dosen/profil-riwayat-saya/simpan/`) -- POST-only, role `tendik` saja, resolve tendik lewat `get_simda_tendik_or_none` (sama seperti `profil_riwayat_saya`), redirect balik ke `profil_riwayat_saya`.
- ✅ **Tab baru "Biodata Saya"** di `detail_tendik.html` -- HANYA tampil saat `mode_diri_sendiri=True` (tidak muncul di view admin `detail_tendik`, yang tetap pakai tombol "Ubah Biodata" penuh lewat `ubah_tendik`). Jadi tab pertama & aktif default untuk tendik, foto ditampilkan sebagai preview bulat + input upload di sampingnya, field lain (nama/NIK/kontak/alamat/dst) dalam grid form biasa.
- Test baru: `simda_dosen/tests.py::SimpanProfilSayaTendikTest` -- dosen ditolak, NIP tidak cocok redirect, DAN yang paling penting: `test_tendik_bisa_ubah_biodata_sendiri_field_admin_tidak_ikut_berubah` membuktikan mengirim `unit_kerja_id`/`nip` (field admin-only) di POST TIDAK mengubah nilainya sama sekali di instance, walau `nama_lengkap` (self-service field) berhasil berubah -- pakai instance `DataTendik` asli (bukan MagicMock) supaya assersi field-level benar-benar valid, dengan `patch.object(DataTendik, 'save')` supaya tidak coba nulis ke koneksi 'simda' sungguhan. **Belum dites di VPS.**
- Tidak ada model/migrasi/SQL SIMDA baru -- `data_tendik` sudah ada sejak fitur "Kelola Data Tendik" awal, murni pakai field yang sudah ada.
- ✅ **Field "Unit Kerja" ditambahkan ke tab Biodata Saya (read-only) — 2026-08-04.** User minta Unit Kerja (referensi tabel `master.unit_kerja` SIMDA, lihat model `simda_dosen.UnitKerja`) ikut tampil di form Biodata Saya, tapi dikonfirmasi lewat `AskUserQuestion` **read-only** (admin yang ubah) -- konsisten dengan alasan `unit_kerja_id` sengaja TIDAK masuk `SELF_SERVICE_FIELDS` di atas (penempatan organisasi/HR, bukan biodata pribadi). Ditambahkan sebagai `<input disabled>` polos menampilkan `{{ tendik.unit_kerja_nama|default:"—" }}` di `detail_tendik.html` -- **BUKAN** bound ke `form_biodata` (tidak dikirim saat submit, tidak ada risiko override lewat POST karena field memang tidak ada di `Meta.fields` ModelForm). Tidak perlu perubahan view/context -- `tendik` sudah tersedia di template lewat `profil_riwayat_saya`/`detail_tendik`. Tidak ada model/migrasi baru.

**[CETAK PDF & EXCEL — DAFTAR DATA TENDIK (Kelola Data Tendik) — selesai per 2026-08-04]** User minta halaman `/simda-dosen/tendik/` (daftar/list, bukan detail satu tendik) bisa diekspor PDF/Excel, sama seperti pola ekspor laporan presensi yang sudah ada. Ini murni ekspor **daftar administratif** (data yang sedang tampil di tabel, termasuk saat sedang difilter kata kunci) -- BUKAN laporan resmi eksternal seperti Laporan Serdos, jadi sengaja **tanpa blok pengesahan/tanda tangan**.
- ✅ **`simda_dosen/tendik_pdf.py::render_pdf_daftar_tendik(daftar, *, kata_kunci="")`** (reportlab, A4 portrait) & **`simda_dosen/tendik_excel.py::render_excel_daftar_tendik(daftar, *, kata_kunci="")`** (openpyxl) -- modul terpisah baru mengikuti pola `render_*` yang sudah dipakai app `presensi` (`laporan_bulanan_pdf.py`/`_excel.py` dkk), bukan ditulis inline di `views.py`. Kolom awalnya sama persis dengan tabel yang tampil di layar (Nama, NIP Yayasan, Jabatan, Unit Kerja, Status Kepegawaian, Status) + nomor urut, plus label kata kunci pencarian kalau sedang difilter -- **kolomnya sudah disesuaikan lagi, lihat catatan "NITK & Pendidikan Terakhir" di bawah.**
- ✅ **`simda_dosen/views.py::_daftar_tendik_qs(request)`** -- helper baru, query+filter `q` yang SAMA dipakai halaman (`daftar_tendik`) & kedua ekspor baru (`export_pdf_daftar_tendik`/`export_excel_daftar_tendik`), supaya hasil ekspor selalu konsisten dengan yang sedang ditampilkan/dicari di layar, bukan logika terpisah yang bisa mencong. Gerbang akses **admin-only** (`_bisa_kelola_data_tendik`) sama seperti halaman daftar itu sendiri -- tidak ada gerbang baru.
- ✅ **URL** (`simda_dosen/urls.py`) -- `tendik/ekspor/pdf/` & `tendik/ekspor/excel/`, ditaruh SEBELUM pattern `tendik/tambah/` (bukan krusial secara teknis karena `<int:tendik_id>` tidak akan cocok dengan `ekspor`, tapi lebih jelas dibaca berurutan).
- ✅ **Template** (`daftar_tendik.html`) -- 2 tombol "Cetak PDF"/"Cetak Excel" (`ti-file-type-pdf`/`ti-file-spreadsheet`, diverifikasi ada di CSS Tabler Icons sebelum dipakai) ditambahkan di sebelah "Tambah Tendik", membawa serta `kata_kunci` (`?q=...`) yang sedang aktif di kotak cari.
- Test baru: `simda_dosen/tests.py::KelolaDataTendikViewTest` ditambah `test_dosen_biasa_tidak_bisa_ekspor_pdf/excel` (403) dan `test_admin_bisa_ekspor_pdf/excel` (200 + Content-Type yang benar) -- pola mock sama dengan `test_admin_bisa_akses_daftar` yang sudah ada. **Belum dites di VPS.**
- Tidak ada model/migrasi baru -- murni fitur tampilan/ekspor dari data yang sudah ada.

**[LABEL "NIP" -> "NITK" + KOLOM LAPORAN Jabatan -> Pendidikan Terakhir — selesai per 2026-08-04]** User minta 2 penyesuaian kosmetik/istilah, bukan perubahan skema: (1) field `nip` (CharField SIMDA asli, DB column TIDAK diubah) di-relabel dari "NIP" jadi "NITK (No Induk Tenaga Kependidikan)" -- istilah yang lebih tepat untuk tendik dibanding "NIP" (istilah PNS/ASN); (2) di laporan/ekspor Daftar Data Tendik, kolom "Jabatan" diganti "Pendidikan Terakhir", dan field NITK (yang tadinya SAMA SEKALI tidak ikut di ekspor, cuma "NIP Yayasan" yang ada) sekarang disertakan.
- ✅ **Label** -- `templates/simda_dosen/tendik_form.html` (field `nip` di form Kelola Data Tendik) diganti jadi "NITK (No Induk Tenaga Kependidikan)". Placeholder kotak cari `daftar_tendik.html` ("Cari nama/NIP/NIP Yayasan") ikut diperbarui jadi "Cari nama/NITK/NIP Yayasan" untuk konsistensi istilah (perilaku pencarian sendiri TIDAK berubah, tetap `Q(nip__icontains=...)`).
- ✅ **Kolom laporan** (`tendik_pdf.py`/`tendik_excel.py`) -- header jadi `No, Nama, NITK, NIP Yayasan, Unit Kerja, Status Kepegawaian, Pendidikan Terakhir, Status` (8 kolom, sebelumnya 7 tanpa NITK dan dengan Jabatan). Pendidikan Terakhir dirender lewat `t.get_pendidikan_terakhir_display()` (field `pendidikan_terakhir` punya `choices`, jadi method ini otomatis tersedia dari Django meski model `managed=False`). Lebar kolom PDF (A4 portrait) direbalance supaya tetap muat (~17.6cm dari 18cm usable width).
- Field `jabatan` (jobdesk sehari-hari) TIDAK dihapus dari model/form Kelola Data Tendik -- cuma tidak lagi jadi salah satu kolom laporan/ekspor, tetap bisa diisi & dilihat lewat halaman Ubah Tendik seperti biasa.
- Tidak ada model/migrasi baru, tidak ada perubahan nama field Python (`nip` tetap `nip`) -- murni perubahan label tampilan + kolom laporan.

**[KELOLA USER: STATUS KEPEGAWAIAN TENDIK, FILTER ROLE, IMPORT EXCEL, RESET PASSWORD MASSAL — selesai per 2026-08-05]** User melaporkan 4 hal setelah mengimpor puluhan akun tendik nyata lewat "Kelola User -> Import Excel": (1) kolom Status Keaktifan/Status Kepegawaian selalu kosong ("-") untuk tendik, (2) password hasil import tidak bisa dipakai login walau sudah "disamakan dengan NIP Yayasan" lewat re-import, (3) dropdown filter Role tidak punya opsi "Tendik", (4) minta semua password tendik direset massal supaya sama dengan usernamenya (memudahkan sosialisasi "username = password").
- ✅ **`simda_dosen/utils.py::attach_kepegawaian_labels`** -- sebelumnya CUMA resolve dosen (lewat `nidn` ke `DataDosen`), tendik selalu dapat `status_kepegawaian_nama`/`jenis_kepegawaian_nama` kosong. Sekarang tendik ikut di-resolve lewat `nip_yayasan` ke `DataTendik` (jenis_map/status_map dari `JenisKepegawaianPublik`/`StatusKepegawaianPublik` dipakai bareng, karena `DataDosen`/`DataTendik` merujuk tabel referensi kepegawaian SIMDA yang sama). ID riset (SINTA/Scopus/dst) & `nama_dosen` TETAP dosen-only (fallback ke nama tendik/akun untuk `nama_dosen`, tapi ID riset kosong untuk tendik -- tidak relevan). `templates/accounts/kelola_user.html` kolom "Status Keaktifan": gate `{% if u.role != 'dosen' %}` diperluas jadi `u.role != 'dosen' and u.role != 'tendik'`. Kolom "Status Kepegawaian" (jenis_kepegawaian_nama) sudah tanpa gate sejak awal, otomatis ikut terisi begitu atributnya ada.
- ✅ **Filter Role** (`kelola_user.html`) -- opsi "Tendik" ditambahkan ke dropdown filter (sebelumnya cuma ada di dropdown Role form Tambah/Ubah User, bukan di filter pencarian daftar).
- ✅ **Root cause password tidak berubah, DITEMUKAN saat investigasi**: `accounts/views.py::import_user` (fitur "Import Excel" di Kelola User) **CUMA CREATE, TIDAK PERNAH UPDATE** -- kalau `username` sudah ada, baris langsung `continue` (dihitung "gagal"), TIDAK PERNAH menimpa password/data yang sudah ada. Jadi mencoba "menyamakan password dengan NIP Yayasan" lewat re-import Excel untuk akun yang sudah dibuat sebelumnya adalah no-op murni -- password lama (apa pun itu) tetap dipakai, sehingga upaya user login pakai NIP Yayasan sebagai password gagal. **Ini bukan bug yang diperbaiki di sesi ini** (mengubah `import_user` jadi upsert adalah perubahan perilaku signifikan, butuh keputusan terpisah) -- solusinya untuk kasus SEKARANG adalah command reset password massal di bawah, BUKAN re-import.
- ✅ **2 bug tambahan ditemukan & diperbaiki di `import_user`/`download_template` (bukan cuma didiagnosis, sekalian dibenahi karena murni penyempurnaan searah dengan perbaikan `tambah_user`/`edit_user` yang sudah ada sebelumnya)**: (a) `valid_roles` (whitelist role saat import) **tidak menyertakan 'tendik'** -- baris Excel dengan `role=tendik` diam-diam diturunkan jadi `role='dosen'` saat import (celah yang sama persis dengan bug "dropdown Role Tambah User" yang sudah diperbaiki 2026-08-01, tapi luput di `import_user` khusus); (b) kolom **NIP Yayasan tidak pernah dibaca dari Excel sama sekali** -- akun tendik hasil import TIDAK PERNAH tertaut ke `DataTendik` SIMDA (nip_yayasan selalu kosong), walau usernamenya kebetulan berupa angka NIP Yayasan. Diperbaiki: `valid_roles` tambah `'tendik'`, kolom ke-11 baru "NIP Yayasan" ditambahkan ke `download_template` (headers, contoh baris tendik, lebar kolom, rentang merge A1:J1→A1:K1 dst) dan dibaca di `import_user` (`row[10]`) lalu disimpan ke `User.objects.create(..., nip_yayasan=...)`.
- ✅ **`accounts/management/commands/reset_password_tendik.py`** (command baru, app `accounts` sebelumnya belum punya folder `management/`) -- reset password SEMUA `User(role='tendik')` supaya sama dengan username masing-masing. **Default dry-run** (cuma menampilkan daftar yang AKAN diubah, tidak menyimpan apa pun), wajib `--yes` untuk benar-benar `set_password()` + `save()`. Dipakai `username` (bukan `nip_yayasan`) sebagai sumber password -- keputusan sadar: `nip_yayasan` bisa saja kosong (lihat bug (b) di atas, termasuk untuk akun-akun tendik yang SUDAH TERLANJUR dibuat sebelum fix ini), sedangkan `username` sudah pasti terisi untuk SEMUA akun & pada praktiknya memang sudah diisi angka NIP Yayasan oleh admin saat membuat akun -- memakai `username` langsung memenuhi tujuan asli user ("username dan password sama") tanpa bergantung pada field yang mungkin belum terisi.
- ✅ **`accounts/views.py::kelola_user` — bug lama ditemukan tidak sengaja, sekalian diperbaiki**: context `'pengaturan': Pengaturan.objects.first()` (dibutuhkan oleh dropdown "Link Riset" SINTA/Scopus/dst di `kelola_user.html`, lihat fitur "Jadikan template URL profil riset jadi pengaturan admin" 2026-08-04) ternyata **tidak pernah benar-benar tersimpan/ter-commit** ke `accounts/views.py` sejak fitur itu dibangun -- `import Pengaturan` + baris context-nya sudah ada di working tree tapi belum pernah masuk git history, artinya dropdown Link Riset di Kelola User kemungkinan besar RUSAK (href kosong) di produksi sejak fitur itu "selesai". Ditemukan & disertakan dalam commit ini saat memeriksa diff `accounts/views.py` untuk memisahkan perubahan sesi ini dari perubahan `refresh_captcha_view` yang tidak terkait (lihat catatan git-staging di bawah).
- Test baru: `presensi/tests.py::AttachKepegawaianLabelsTendikTest` (mock SIMDA, pola sama `AttachNamaResmiTest`), `accounts/tests.py::ImportUserTendikTest` (upload file Excel in-memory via openpyxl, verifikasi role & nip_yayasan tersimpan), `accounts/tests.py::ResetPasswordTendikCommandTest` (dry-run tidak mengubah, `--yes` mengubah tendik tapi TIDAK menyentuh dosen) -- **belum dites di VPS** (Postgres lokal tidak bisa diakses, lihat Bagian 8).
- ⚠️ **Catatan git-staging**: `accounts/urls.py`/`accounts/views.py` masih punya perubahan `refresh_captcha_view` yang sudah lama nangkring uncommitted (bukan punya sesi ini) -- di-exclude dari commit ini lewat `git add -p` per hunk, sama pola dengan sesi-sesi sebelumnya.
- Tidak ada model/migrasi baru -- perubahan murni di layer views/utils/template + 1 management command baru.
- ✅ **`accounts/management/commands/backfill_nip_yayasan_tendik.py`** (command baru, ditambahkan segera setelah reset password di atas) -- akun tendik yang dibuat SEBELUM fix kolom NIP Yayasan di `import_user` (lihat poin di atas) tetap punya `nip_yayasan` kosong walau `username`-nya sendiri sudah berupa angka NIP Yayasan. Command ini mengisi `nip_yayasan` yang MASIH KOSONG dengan nilai `username` masing-masing, khusus `role='tendik'` -- TIDAK menimpa yang sudah terisi (mis. beda dari username), TIDAK menyentuh role lain. Default dry-run, `--yes` untuk menerapkan (pola sama `reset_password_tendik`). Ini melengkapi `attach_kepegawaian_labels` (Status Kepegawaian tendik) dan self-service Riwayat/Biodata Tendik -- semuanya bergantung pada `nip_yayasan` terisi benar untuk bisa resolve ke `DataTendik` SIMDA.
- Test baru: `accounts/tests.py::BackfillNipYayasanTendikCommandTest` (dry-run tidak mengubah, `--yes` mengisi yang kosong, tidak menimpa yang sudah terisi, tidak menyentuh role lain) -- **belum dites di VPS**.

**[BUG NYATA 500 DI PRODUKSI: SIMPAN BIODATA SAYA TENDIK — selesai per 2026-08-05]** User melaporkan `Server Error (500)` saat tendik menyimpan "Biodata Saya" (`/simda-dosen/profil-riwayat-saya/simpan/`). Traceback asli diambil dari `sudo tail /var/www/sikd/logs/error.log` (produksi `DEBUG=False`, halaman cuma tampil generik) -- akar masalah:
```
ValueError: invalid literal for int() with base 10: ''
...
ValueError: Field 'agama_id' expected a number but got ''.
```
- ✅ **Root cause**: `forms.TypedChoiceField(coerce=int, required=False, ...)` TANPA `empty_value=None` -- default bawaan Django untuk `TypedChoiceField.empty_value` adalah **string kosong `''`**, bukan `None`. Saat field dikosongkan di form, `_coerce()` mengembalikan `''` (bukan `None`), lalu `form.save()` mencoba `int('')` untuk field model `agama_id` (`IntegerField(null=True)`) -- meledak dengan `ValueError` yang TIDAK tertangkap di mana pun (bukan `ValidationError`, jadi lolos dari `form.is_valid()` dan baru meledak saat `instance.save()` sungguhan menulis ke Postgres).
- ✅ **Diperbaiki di 2 form, 6 field total** (pola identik disalin sebelumnya, jadi harus diperbaiki di semua tempat sekaligus): `simda_dosen/forms.py::ProfilSayaTendikForm.agama_id` (yang benar-benar memicu error user) DAN `DataTendikForm.unit_kerja_id`/`jenis_kepegawaian_id`/`status_kepegawaian_id`/`golongan_id`/`agama_id` (bug LATEN yang sama persis, dipakai admin lewat Tambah/Ubah Tendik -- belum sempat ke-trigger di laporan user, tapi pasti akan meledak juga begitu admin mengosongkan salah satu dropdown itu). Semua ditambah `empty_value=None`.
- ⚠️ **Kenapa lolos dari test yang sudah ada**: `SimpanProfilSayaTendikTest::test_tendik_bisa_ubah_biodata_sendiri_field_admin_tidak_ikut_berubah` sudah ada sebelumnya, tapi pakai `@patch.object(DataTendik, "save", new=MagicMock())` -- method `.save()` di-mock TOTAL, jadi write sungguhan ke Postgres (tempat `ValueError`-nya benar-benar muncul) TIDAK PERNAH dijalankan di test itu, bug-nya sama sekali tidak terdeteksi lewat mock ini. Pelajaran: mock `.save()` cocok untuk menguji field mana yang BOLEH berubah (level Python object), tapi TIDAK bisa menangkap bug yang munculnya justru di lapisan serialisasi-ke-DB (`get_prep_value`, type coercion, dst).
- ✅ **Test regresi baru** (`simda_dosen/tests.py::ProfilSayaTendikFormAgamaKosongTest`) -- murni level Form (`form.is_valid()` + `form.cleaned_data`), TIDAK panggil `.save()` sama sekali, jadi tidak butuh DB tapi tetap menangkap kelas bug ini secara langsung (assert `cleaned_data['agama_id'] is None`, bukan `''`). 2 test: `ProfilSayaTendikForm.agama_id` (kasus nyata yang dilaporkan) dan kelima `TypedChoiceField` `DataTendikForm` sekaligus (bug laten yang belum ke-trigger).
- Tidak ada model/migrasi baru -- murni bug fix di layer form (tipe data), diverifikasi manual lewat script Python (`form.is_valid()`/`cleaned_data`, Postgres lokal tidak bisa diakses untuk test suite penuh) sebelum push -- **belum dites di VPS lewat `manage.py test`, tapi behavior yang memicu 500 sudah diverifikasi hilang manual**.

**[BUG: SEMUA FIELD TANGGAL DI KELOLA TENDIK TERLIHAT "TIDAK TERSIMPAN" — selesai per 2026-08-05]** User minta dicek semua field bertipe tanggal di Kelola Tendik setelah melaporkan Tanggal Lahir "tidak tersimpan". Bug ini BERBEDA dari bug agama_id di atas (yang 500 saat SIMPAN) -- ini soal field tanggal terlihat KOSONG saat form DIBUKA UNTUK EDIT walau datanya sudah tersimpan benar di database, dan kalau form itu di-submit ulang tanpa tanggalnya disentuh, nilai kosong itu MENIMPA tanggal yang benar jadi NULL sungguhan.
- ✅ **Root cause (diverifikasi presisi lewat pembacaan source Django, bukan cuma dugaan)**: `forms.DateInput(attrs={"type": "date"})` TANPA `format="%Y-%m-%d"` eksplisit merender value existing pakai `formats.get_format("DATE_INPUT_FORMATS")[0]` untuk locale AKTIF (`LANGUAGE_CODE = 'id-id'`) -- format Indonesia bawaan Django dimulai dari `"%d-%m-%Y"` (mis. "25-12-1985"), BUKAN ISO 8601. Tapi `<input type="date">` HTML5 CUMA menerima value persis format ISO (`YYYY-MM-DD`) -- kalau tidak cocok, browser diam-diam menampilkan field itu KOSONG (bukan error), memberi kesan "tanggal tidak tersimpan" walau sebenarnya tersimpan benar.
- ⚠️ **Percobaan pertama yang GAGAL (dicoba & di-revert, dicatat supaya tidak diulang)**: awalnya coba perbaiki dengan menambah `DATE_INPUT_FORMATS = ['%Y-%m-%d', ...]` di `config/settings.py` (site-wide fix). **TERBUKTI TIDAK BERPENGARUH SAMA SEKALI** setelah diverifikasi lewat `formats.get_format('DATE_INPUT_FORMATS')` langsung -- dibaca source `django/utils/formats.py::get_format()`: selama localization aktif (default), Django mengambil `DATE_INPUT_FORMATS` dari **locale format module** (`django/conf/locale/id/formats.py`) LEBIH DULU, dan `settings.DATE_INPUT_FORMATS` HANYA dipakai kalau locale module TIDAK mendefinisikannya sama sekali -- locale `id` SUDAH mendefinisikannya, jadi setting global ini SELALU KALAH/diabaikan. (Menariknya, Django OTOMATIS menambahkan `%Y-%m-%d` ke AKHIR daftar format PARSING lewat mekanisme `ISO_INPUT_FORMATS` -- artinya submit tanggal baru lewat date picker sebenarnya SUDAH bisa di-parse dengan benar sejak awal; yang benar-benar rusak HANYA render tampilan `format_value()`, yang memakai indeks `[0]` dari daftar locale, bukan hasil auto-append itu.)
- ✅ **Fix yang BENAR-benar berpengaruh (diverifikasi via `widget.format_value()` langsung)**: tambahkan `format="%Y-%m-%d"` eksplisit ke ARGUMEN WIDGET (bukan attrs, parameter constructor `DateInput(attrs=..., format=...)`) -- parameter `format` eksplisit di widget MEMOTONG lookup locale sama sekali (`self.format or formats.get_format(...)`). Diterapkan ke SEMUA 6 widget `DateInput(attrs={"type": "date"})` yang ada di `simda_dosen/forms.py`: `DataTendikForm.tgl_lahir`/`tgl_mulai_kerja`/`tgl_sk_pengangkatan`, `RiwayatPelatihanTendikForm.tanggal_mulai`/`tanggal_selesai`, `ProfilSayaTendikForm.tgl_lahir` -- ini mencakup SEMUA field tanggal di Kelola Tendik (Riwayat Pendidikan pakai `tahun_masuk`/`tahun_lulus` IntegerField, Riwayat Prestasi pakai `tahun` IntegerField, keduanya BUKAN DateField jadi tidak kena bug ini).
- Test baru: `simda_dosen/tests.py::TendikDateInputFormatTest` -- 3 test memverifikasi `form['field'].value()` mengembalikan string ISO (`"1985-12-25"`) untuk instance yang sudah punya tanggal tersimpan, mencakup ketiga form (`ProfilSayaTendikForm`, `DataTendikForm`, `RiwayatPelatihanTendikForm`) -- diverifikasi manual lewat script Python (`form['field'].value()` dengan locale `id-id` diaktifkan) sebelum push karena Postgres lokal tidak bisa diakses untuk `manage.py test`.
- ⚠️ **Bug serupa KEMUNGKINAN ada di tempat lain di portal** yang memakai `forms.DateInput(attrs={"type": "date"})` tanpa `format=` eksplisit (mis. `presensi/forms.py` juga punya pola yang sama) -- TIDAK ikut diperbaiki di sesi ini karena scope permintaan user spesifik "Kelola Tendik", tapi perlu diwaspadai kalau ada laporan serupa ("tanggal tidak tersimpan") di modul lain.
- Tidak ada model/migrasi baru -- murni bug fix widget-level di layer form.

**[KOMPRESI OTOMATIS SEMUA UPLOAD FILE (500KB) — selesai per 2026-08-05]** User minta semua fitur upload dikompres otomatis, sama seperti yang sudah ada di role dosen (`simda_dosen/file_compress.py::compress_uploaded_file`, dipakai `profil/views.py` & `kinerja/views.py` sejak sebelumnya -- gambar JPG/PNG lewat Pillow, PDF lewat PyMuPDF, target default ~480KB, jatuh balik ke file asli kalau tipe tidak didukung/gagal kompres/sudah cukup kecil).
- ✅ **Kelola Tendik (8 titik upload, `simda_dosen/views.py`)** -- SEMUA-nya sebelumnya ModelForm murni (`form.save()` langsung, tidak ada langkah kompresi sama sekali, beda dari Profil Dosen yang HTML manual jadi sudah lebih dulu terpasang): `foto` di `tambah_tendik`/`ubah_tendik` (`DataTendikForm`) & `simpan_profil_saya_tendik` (`ProfilSayaTendikForm`), `file_ijazah` di tambah/edit Riwayat Pendidikan, `file_sertifikat` di tambah/edit Riwayat Pelatihan, `file_bukti` di tambah/edit Riwayat Prestasi. Pola: setelah `form.is_valid()`, sebelum `form.save()`, timpa `form.instance.<field> = compress_uploaded_file(request.FILES['<field>'])` kalau field itu ada di `request.FILES` -- aman dipasang di atas ModelForm karena `_post_clean()` (dipanggil `is_valid()`) sudah mengisi `form.instance` duluan, jadi override manual di sini menang sebelum `.save()` benar-benar menulis.
- ✅ **Lampiran Izin/Cuti (`presensi/views.py::halaman_izin`)** -- `IzinCuti.lampiran` (scan surat sakit dkk, `IzinCutiForm`) ikut ditambahkan, pola sama persis.
- ⚠️ **SENGAJA TIDAK ikut dikompres, dengan alasan teknis (bukan lupa)**:
  - `ParafDosen.gambar` (`presensi/views.py::simpan_paraf`, lewat DRF serializer bukan Django Form) -- paraf hasil gambar canvas (garis tangan di atas putih polos), ukurannya sudah sangat kecil secara alami (bukan foto/scan), dan `compress_uploaded_file` mengonversi ke JPEG -- akan menghilangkan transparansi PNG (kalau dipakai) dan menambah artefak kompresi pada garis tanda tangan yang seharusnya tajam, merugikan kualitas untuk dokumen resmi (Laporan Serdos) tanpa manfaat ukuran yang berarti.
  - `Presensi.berkas` (selfie presensi masuk/pulang, verifikasi wajah) -- field model INI ADA tapi TERNYATA TIDAK PERNAH DIISI di kode manapun (dicek `grep`, tidak ada `.berkas =` sama sekali) -- selfie yang di-capture untuk cek wajah diproses LANGSUNG di memori jadi face embedding (`presensi/face.py`), tidak pernah disimpan sebagai file fisik lewat field ini. Tidak ada yang perlu dikompres karena tidak ada write ke field itu untuk dicegat.
- ⚠️ **Bug LAIN ditemukan tidak sengaja saat menelusuri file ini (dicatat, TIDAK diperbaiki -- di luar scope kompresi)**: `presensi/forms.py::IzinCutiForm` punya widget `tanggal_mulai`/`tanggal_selesai` dengan pola bug tanggal PERSIS SAMA seperti yang baru diperbaiki di Kelola Tendik (`DateInput` tanpa `format="%Y-%m-%d"`) -- kemungkinan besar mengalami gejala yang sama ("tanggal terlihat kosong saat edit"), tapi form Izin/Cuti ini cuma dipakai untuk PENGAJUAN BARU (tidak ada alur edit tanggal yang sudah ada), jadi dampaknya lebih kecil (tidak ada risiko data lama tertimpa NULL) -- tetap perlu diperbaiki kalau nanti ada fitur edit pengajuan izin.
- Test baru: `simda_dosen/tests.py::CompressUploadedFileWiringTest` (3 test: `tambah_tendik` foto, `simpan_profil_saya_tendik` foto, tambah Riwayat Pendidikan file_ijazah -- pola representatif, ke-8 titik pakai pola identik jadi tidak semua dites satu-satu), `presensi/tests.py::HalamanIzinTest::test_lampiran_dikompres_sebelum_disimpan` -- semua murni verifikasi WIRING (mock `compress_uploaded_file`, assert dipanggil dengan file yang benar), bukan logika kompresi itu sendiri (tanggung jawab `file_compress.py`, sudah dipakai lama tanpa test khusus). **Belum dites di VPS.**
- Tidak ada model/migrasi baru -- murni pemanggilan utility yang sudah ada ke titik-titik upload yang belum memakainya.

**[AVATAR FOTO PROFIL DI DAFTAR KELOLA DATA TENDIK — selesai per 2026-08-05]** User minta ikon avatar foto profil di depan nama tendik, meniru pola yang sudah ada di Rekap Data dosen (`templates/dashboard/rekap.html`).
- ✅ **`templates/simda_dosen/daftar_tendik.html`** -- kolom Nama sekarang menampilkan foto bulat 36px (`<img>`, `border-radius:50%`, `object-fit:cover`) di depan nama kalau `t.foto` terisi, fallback ke ikon `ti-user-circle` abu-abu kalau kosong -- markup & ukuran disesuaikan (36px vs 42px di Rekap Dosen, tabel ini sedikit lebih padat) tapi pola CSS/fallback-nya identik. Tidak ada perubahan view -- `DataTendik.foto` (ImageField) sudah tersedia di `daftar` yang dikirim `daftar_tendik` sejak awal.
- ⚠️ **Catatan teknis test (bug MagicMock, bukan bug aplikasi)**: test awal pakai `MagicMock()` untuk item daftar tiruan GAGAL SILUMAN (assertion salah lolos/gagal tanpa terdeteksi) -- Django `Variable._resolve_lookup` mencoba dictionary-lookup (`t['foto']`) LEBIH DULU sebelum attribute-lookup, dicek lewat `hasattr(type(t), '__getitem__')` (level CLASS, bukan instance) -- `MagicMock` (beda dari `Mock` biasa) SELALU punya `__getitem__` di level class (auto-implementasi semua magic method), jadi `t['foto']` selalu "berhasil" (mengembalikan mock bersarang) dan `getattr(t, 'foto')` yang sudah di-set manual TIDAK PERNAH tercapai. Diperbaiki dengan pakai `Mock()` biasa (bukan `MagicMock()`) untuk item yang di-render lewat template -- `MagicMock` tetap dipakai seperti biasa untuk mocking chain query (`.objects.using().all().order_by()`) yang tidak lewat resolusi variabel Django. Diverifikasi manual lewat `render_to_string()` langsung (dengan `RequestFactory` supaya context processor `base.html` terpenuhi) sebelum push, karena Postgres lokal tidak bisa diakses untuk `manage.py test`.
- Test baru: `simda_dosen/tests.py::KelolaDataTendikViewTest::test_avatar_foto_tampil_di_daftar` -- 2 skenario dalam satu request (satu item dengan foto, satu tanpa) memverifikasi `<img>` DAN fallback ikon sama-sama muncul di respons. **Belum dites di VPS.**
- Tidak ada model/migrasi/view baru -- murni tambahan tampilan di satu template.

**[FOTO PROFIL DI DASHBOARD TENDIK — selesai per 2026-08-05]** User minta foto profil ditambahkan ke dashboard tendik, meniru pola dashboard dosen (kartu "Profil Saya" di `templates/dashboard/index.html`, kelas CSS `.avatar`/`.avatar-placeholder`).
- ✅ **`templates/dashboard/index.html`, cabang `role == 'tendik'`** -- berbeda dari dosen (yang punya kartu "Profil Saya" TERPISAH di bawah stat cards), dashboard tendik tidak punya kartu profil sejenis, jadi avatar disisipkan LANGSUNG ke banner sambutan (`welcome-card-tendik`) yang sudah ada -- diubah dari teks-saja jadi flex layout (avatar + teks sejajar). Kelas CSS baru `.avatar-tendik`/`.avatar-placeholder-tendik` (bukan reuse `.avatar`/`.avatar-placeholder` milik dosen -- konsisten dengan konvensi penamaan bersufiks "-tendik" yang sudah dipakai cabang ini, meski secara teknis tidak akan bentrok karena cuma satu cabang yang pernah dirender per request). Sumber foto: `tendik_profil.foto` (`DataTendik.foto`, sudah ada di `SELF_SERVICE_FIELDS`, context var `tendik_profil` sudah ada sejak fitur Dashboard Tendik awal) -- fallback ikon `ti-user` kalau `tendik_profil` `None` (belum tertaut SIMDA) ATAU field `foto`-nya kosong.
- Test baru: `dashboard/tests.py::DashboardTendikTest::test_tanpa_tendik_profil_tampil_placeholder_avatar` & `test_tendik_dengan_foto_tampil_avatar` (mock `get_simda_tendik_or_none`, pakai `Mock()` biasa bukan `MagicMock()` -- alasan sama dengan catatan bug MagicMock di atas). Diverifikasi manual lewat render snippet langsung (3 skenario: ada foto/profil None/profil ada tapi foto kosong) sebelum push. **Belum dites di VPS.**
- Tidak ada model/migrasi/view baru -- murni tambahan tampilan di satu template (`tendik_profil` sudah tersedia di context sejak awal).

**[LAPORAN PDF/EXCEL DAFTAR DATA TENDIK: LANDSCAPE + WRAP TEKS — selesai per 2026-08-05]** User kirim screenshot PDF Daftar Data Tendik dengan teks tumpang-tindih antar kolom (nama panjang menimpa NITK, "Administrasi Fakultas Hukum" menimpa Unit Kerja) -- akar masalah: `simda_dosen/tendik_pdf.py` sebelumnya mengisi sel `reportlab.Table` dengan STRING POLOS, yang TIDAK PERNAH wrap ke baris baru (beda dari `Paragraph` yang bisa) -- teks lebih lebar dari kolom cuma "meluber" tumpang tindih ke sel sebelah, bukan error atau terpotong bersih.
- ✅ **`simda_dosen/tendik_pdf.py`** -- (1) `pagesize=A4` diganti `pagesize=landscape(A4)` (lebar cetak ~26.7cm setelah margin, dari sebelumnya ~18cm portrait); (2) SEMUA sel data dibungkus `Paragraph` (gaya baru `sel_style`/`sel_tengah_style`, fontSize 8) alih-alih string polos -- reportlab otomatis wrap teks & menambah tinggi baris kalau perlu (2 baris dst), TIDAK PERNAH lagi tumpang tindih ke sel lain; (3) lebar kolom di-rebalance memanfaatkan ruang landscape yang lebih lega (Nama 3.2→4.5cm, Unit Kerja 2.4→4.5cm, Status Kepegawaian 2.5→3.5cm, dst).
- ✅ **`simda_dosen/tendik_excel.py`** -- kolomnya SUDAH pakai `wrap_text=True` sejak awal (bukan bug di sisi Excel), jadi cuma ditambah `page_setup.orientation = "landscape"` + `fitToWidth=1` (konsisten cetak landscape kalau file ini di-print, bukan cuma dibuka di layar) dan kolom Nama/Unit Kerja/Status Kepegawaian dilebarkan sedikit (28→32, 20→28, 22→24) mengikuti proporsi PDF yang baru.
- Diverifikasi visual: PDF contoh di-generate dengan data realistis (nama+gelar panjang, "Administrasi Fakultas Hukum") lewat script manual, dikirim ke user lewat `SendUserFile`, dikonfirmasi user rapi sebelum di-push -- bukan cuma diverifikasi lewat assertion status-code seperti biasanya (Postgres lokal tidak bisa diakses untuk `manage.py test`, dan lebih penting lagi untuk perbaikan VISUAL murni seperti ini, cara PALING pasti adalah dilihat langsung, bukan dites otomatis).
- Test lama (`test_admin_bisa_ekspor_pdf`/`test_admin_bisa_ekspor_excel`, cuma cek status code + Content-Type) TIDAK perlu diubah -- perubahan ini murni presentasi/layout, bukan data yang diekspor.
- Tidak ada model/migrasi baru -- murni perbaikan layout PDF/Excel.
